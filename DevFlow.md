# Repeatable AI-Assisted Development Flow

## Purpose

This document is a living, platform-neutral playbook for building software with AI assistants such as Claude, Cursor, Codex, or future tools. It defines the repeatable process, required artifacts, quality gates, and human/AI responsibilities that should carry across projects.

The objective is not only to ship an application. It is to create a development system that makes future applications easier to plan, build, verify, operate, and hand over.

## Core principles

1. **The repository is the source of truth.** Important requirements, decisions, commands, and progress must live in version-controlled files rather than only in an AI chat.
2. **Human intent stays explicit.** The human owns goals, priorities, risk acceptance, credentials, permissions, and final product decisions.
3. **AI work must be reviewable.** Ask the AI to make scoped changes, explain important tradeoffs, run relevant checks, and report exactly what changed.
4. **Build vertical slices early.** Prove one end-to-end path before adding breadth or infrastructure.
5. **Use evidence-based gates.** A phase is complete only when its acceptance criteria are demonstrated by tests, deployed behavior, or reviewed artifacts.
6. **Prefer reversible decisions.** Use interfaces, configuration, and small commits where requirements are likely to change.
7. **Security and operations are design inputs.** Authentication, authorization, isolation, secrets, logging, recovery, and cost controls must not be deferred until the end.
8. **Record learning continuously.** Capture mistakes, successful prompts, useful commands, rejected approaches, and reusable patterns while they are fresh.
9. **Bound slow operations and communicate early.** Give routine external commands a short observation window, report unexpected delay immediately, and cancel or bypass nonessential diagnostics instead of leaving the human without an update. Long-running work needs visible progress and an explicit stop condition.

## The reusable lifecycle

```text
Frame -> Discover -> Decide -> Scaffold -> Prove -> Iterate -> Harden -> Release -> Learn
                         ^                    |                         |
                         +--------------------+-------------------------+
```

Every phase ends with a gate. If the evidence for a gate is missing, keep the phase open or explicitly record the accepted risk.

## Phase 0: Frame the problem

### Goal

Turn the initial idea into a bounded problem with measurable outcomes.

### Required artifacts

- One-paragraph problem statement.
- Target users and their main jobs.
- In-scope and out-of-scope lists.
- Functional requirements.
- Non-functional requirements such as security, latency, availability, privacy, and expected scale.
- Success measures.
- Known constraints and assumptions.

### AI workflow

- Ask the AI to identify ambiguities, conflicting requirements, hidden dependencies, and likely risks.
- Ask it to restate the requirements as testable outcomes.
- Do not ask for implementation until the important unknowns are visible.

### Gate

- [ ] The problem and target user are clear.
- [ ] The initial release boundary is explicit.
- [ ] Success can be evaluated objectively.
- [ ] Major assumptions are recorded.

## Phase 1: Discover the environment

### Goal

Confirm the real constraints of the target platform, repository, APIs, data, team, and deployment environment.

### Required artifacts

- Environment and dependency inventory.
- API and integration inventory.
- Authentication and authorization matrix.
- Data classification and retention needs.
- Platform entitlements, quotas, and cost assumptions.
- List of unresolved questions with owners.

### AI workflow

- Let the AI inspect the repository and available configuration before proposing changes.
- Require current primary documentation for changing platform capabilities.
- Separate verified facts from assumptions and recommendations.
- Never paste production secrets into prompts or repository files.

### Gate

- [ ] Required services and permissions are available.
- [ ] External dependencies and authentication flows are understood.
- [ ] Blocking unknowns have owners or experiments.
- [ ] Secret-management and local-development approaches are defined.

## Phase 2: Decide the architecture

### Goal

Choose the smallest architecture that satisfies the requirements and leaves intentional extension points.

### Required artifacts

- System-context and component diagrams.
- Technology decision table.
- Data ownership and trust boundaries.
- Deployment topology.
- Failure-mode and scaling assumptions.
- Architecture Decision Records for important or difficult-to-reverse choices.

### AI workflow

- Ask for two or three viable options with concrete tradeoffs.
- Challenge unnecessary services, frameworks, and abstractions.
- Ask how identity, data, failures, retries, and observability move across component boundaries.
- Record the chosen option and rejected alternatives in an ADR.

### Gate

- [ ] Every requirement maps to an architectural component or policy.
- [ ] Trust boundaries and data ownership are explicit.
- [ ] Major technology choices have written rationale.
- [ ] The first vertical slice is identified.

## Phase 3: Establish the repository and delivery foundation

### Goal

Create a reproducible project that another developer or AI assistant can understand and run.

### Recommended baseline

- `README.md` for purpose, setup, run, test, and deployment instructions.
- `DevFlow.md` for this lifecycle and project progress.
- `docs/architecture.md` for the current design.
- `docs/decisions/` for ADRs.
- `docs/product.md` for requirements and release scope.
- `.env.example` containing names and safe examples, never secrets.
- Automated format, lint, type-check, test, build, and security commands.
- A deterministic dependency lockfile.
- CI that runs the same checks used locally.

### AI workflow

- Ask the AI to inspect existing conventions before scaffolding.
- Prefer standard framework generators and documented platform conventions.
- Require one command for each common operation where practical.
- Verify generated files; do not treat generation as evidence that the application works.

### Gate

- [ ] A clean checkout can be configured from documented steps.
- [ ] Local checks run through documented commands.
- [ ] CI runs equivalent checks.
- [ ] No secrets, generated build output, or machine-specific files are committed.

## Phase 4: Prove one vertical slice

### Goal

Deliver the thinnest end-to-end path through the real architecture.

The first slice should normally include the user entry point, authentication, backend request, persistence or integration where applicable, error handling, logging, tests, and deployment.

### AI workflow

- Give the AI one user-visible outcome at a time.
- Ask it to state the files it expects to touch and the verification it will run.
- Use mocks only at boundaries that cannot yet be reached; clearly mark them.
- Test the deployed path, not only local functions.
- For authentication changes, test the complete provider redirect and callback with a real user; an initial login redirect alone is not proof of a working flow.

### Gate

- [ ] A real user can complete one useful workflow.
- [ ] Authorization is enforced on the backend.
- [ ] Failure behavior is visible and understandable.
- [ ] The path has automated tests and a deployment smoke test.

## Phase 5: Iterate in small feature loops

For each feature, repeat this loop:

1. Define the user outcome and acceptance criteria.
2. Inspect the relevant code and documentation.
3. Identify design impact and risks.
4. Write or update the implementation plan.
5. Implement the smallest coherent change.
6. Run focused tests, then broader regression checks.
7. Review the diff for correctness, security, and unnecessary complexity.
8. Update documentation and decisions.
9. Commit a coherent change.
10. Demonstrate the feature against its acceptance criteria.

### Feature gate

- [ ] Acceptance criteria pass.
- [ ] Relevant tests exist and pass.
- [ ] Error, authorization, and edge cases were considered.
- [ ] Logs do not expose secrets or inappropriate user data.
- [ ] Documentation matches the implemented behavior.
- [ ] The change can be rolled back or disabled safely.

## Phase 6: Harden the system

### Security

- Threat-model authentication, authorization, session isolation, API access, uploads, agent tools, and administrative functions.
- Apply least privilege and server-side authorization.
- Keep credentials in the platform secret store or approved CI secret store.
- Add rate limits, timeouts, payload limits, dependency scanning, and audit events.
- Treat external API, model, and tool output as untrusted input.

### Reliability

- Define timeouts, retries, idempotency, circuit breakers, and degradation behavior.
- Ensure application instances are stateless where horizontal scaling is expected.
- Test dependency failures and partial outages.
- Verify backup, restore, migration, and rollback procedures.

### Quality

- Run unit, integration, contract, end-to-end, security, and load tests appropriate to the risks.
- Test user and tenant isolation explicitly.
- Check accessibility and supported browsers for user interfaces.

### Gate

- [ ] Threats and mitigations are reviewed.
- [ ] Critical failure modes have tests or runbooks.
- [ ] Performance is acceptable at expected load.
- [ ] Backup, restore, and rollback paths are known.

## Phase 7: Release deliberately

### Required artifacts

- Versioned release candidate.
- Release notes.
- Environment-specific configuration checklist.
- Database migration and rollback plan.
- Deployment verification checklist.
- Operational dashboard and alert list.
- Known limitations.

### Gate

- [ ] CI is green on the exact release revision.
- [ ] Required approvals are recorded.
- [ ] Production configuration contains no development defaults.
- [ ] Smoke tests pass after deployment.
- [ ] Rollback criteria and responsible person are known.

## Phase 8: Operate and learn

### Goal

Use production evidence to improve both the product and the development process.

Track:

- User outcomes and adoption.
- Errors, latency, availability, and dependency health.
- Security and audit events.
- AI token usage and cost where applicable.
- Escaped defects and their root causes.
- Manual work that should become automation.

After each milestone, update the learning log:

```markdown
### YYYY-MM-DD — Milestone or incident

- What worked:
- What did not work:
- Evidence:
- Decision or process change:
- Reusable pattern:
- Follow-up owner:
```

## Platform-neutral AI collaboration protocol

### Start every AI work session with a task brief

```markdown
## Objective

What concrete outcome should exist when this task is done?

## Context

Which repository, feature, documentation, and prior decisions matter?

## Scope

What may the AI read, change, run, or deploy?

## Constraints

What technologies, compatibility, security, and style rules must be followed?

## Acceptance criteria

What observable evidence proves completion?

## Verification

Which tests, builds, reviews, or deployed checks are required?

## Exclusions

What must not be changed?
```

### Require this operating behavior from any AI assistant

1. Inspect before editing.
2. State assumptions and important risks.
3. Preserve unrelated work.
4. Keep changes within the authorized scope.
5. Use existing project conventions and primary documentation.
6. Run relevant verification after changes.
7. Report changed files, tests, remaining risks, and manual steps.
8. Never claim success without evidence.

### End every AI work session with a handoff

```markdown
## Completed

- User-visible and technical outcomes.

## Changed

- Files, configuration, dependencies, and infrastructure.

## Verified

- Commands and results.

## Decisions

- New ADRs or important tradeoffs.

## Remaining

- Open work, risks, and blockers.

## Recommended next step

- The smallest useful next action.
```

This handoff format makes work portable between AI platforms because the project state is written into the repository instead of depending on one assistant's conversation history.

### Maintain a problem-solving audit trail

For every material problem, incident, or blocked implementation, maintain a dated problem record in this file while the work is happening. The record must give the human reviewer a clear window into the observable evidence, alternatives considered, actions taken, and reasons for each decision. It must not depend on access to a particular AI chat or expose credentials, tokens, personal data, or private hidden model reasoning.

Use this structure:

```markdown
### YYYY-MM-DD — Problem title

- Objective:
- Initial symptom:
- Constraints and approval boundary:
- Evidence collected:

| Sequence | Hypothesis or question | Action or experiment | Result | Decision or next step |
| -------- | ---------------------- | -------------------- | ------ | --------------------- |
| 1        |                        |                      |        |                       |

- Root cause:
- Final change:
- Verification:
- Incorrect or incomplete assumptions corrected:
- Reusable lessons:
- Remaining risks or follow-up:
```

Audit-trail rules:

1. Record failed attempts as well as successful ones; do not rewrite history to make the solution appear linear.
2. Distinguish facts, hypotheses, inferences, and user-provided information.
3. Record relevant commands, deployment versions, files, and external primary sources when they make the result reproducible.
4. State when a diagnostic method was invalid or inconclusive and how that was discovered.
5. Never claim a root cause until the final behavior or a focused experiment supports it.
6. Keep the record platform-neutral where possible, and move broadly reusable lessons into the lifecycle rules.
7. Update the record in the background as part of the currently approved task; do not wait until details have been lost.

### Require human approval between phases

Finishing one phase, milestone, or feature does not authorize the AI assistant to begin the next one. At every transition, the assistant must stop and provide:

- what was completed and the evidence;
- important decisions and corrected assumptions;
- unresolved questions, risks, and dependencies;
- the proposed scope and acceptance criteria for the next phase;
- questions that need human input.

The next codebase phase or feature starts only after explicit human approval. Read-only inspection or discussion requested by the human is allowed, but must not silently expand into implementation. Documentation, verification, and audit-trail updates needed to finish the already approved task remain part of that task.

## Git working agreement

- Keep the default branch releasable.
- Use short-lived branches for coherent changes.
- Prefer small commits with clear intent.
- Do not combine unrelated formatting, refactoring, and feature work.
- Review the staged diff before committing.
- Reference requirements, issues, or ADRs when useful.
- Do not commit secrets, local credentials, generated packages, or AI chat transcripts containing sensitive information.

Suggested commit prefixes:

```text
feat:     user-visible capability
fix:      defect correction
docs:     documentation only
test:     test changes
refactor: behavior-preserving code change
build:    dependencies or build system
ci:       delivery automation
chore:    maintenance
```

## Definition of Done

A task is done only when:

- The requested behavior is implemented.
- Acceptance criteria are demonstrated.
- Relevant automated checks pass.
- Authorization, privacy, errors, and edge cases were considered.
- Documentation and configuration examples are current.
- No secrets or unrelated changes are included.
- The result is understandable to a new developer or a different AI assistant.
- Remaining limitations and risks are explicitly recorded.

## BTPApp progress tracker

| Phase                | Status                  | Evidence or next gate                                                                                                                                                    |
| -------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 0. Frame             | Complete                | Product scope, users, constraints, MVP boundary, and success criteria are in `docs/product.md`.                                                                          |
| 1. Discover          | In progress             | BTP Cloud Foundry target and core trial services verified; target API authentication, model access, and the first OpenAPI YAML remain.                                   |
| 2. Decide            | Complete for foundation | Architecture and initial XSUAA decisions are recorded in ADRs 0001 and 0002.                                                                                             |
| 3. Foundation        | Complete for foundation | GitHub remote, modules, locked dependencies, MTA build, CI workflow, initial commit, and push are established.                                                           |
| 4. Vertical slice    | In progress             | Checkpoints 1 through 3 are approved. Checkpoint 3A is proceeding under a temporary autonomous fast-track with a full audit trail; live deployment remains Checkpoint 4. |
| 5. Iterate           | Not started             | Begin after the first deployed vertical slice.                                                                                                                           |
| 6. Harden            | Not started             | Threat model and production checks pending.                                                                                                                              |
| 7. Release           | Not started             | Release process pending.                                                                                                                                                 |
| 8. Operate and learn | Not started             | Metrics and milestone retrospectives pending.                                                                                                                            |

## FlowPilot application startup runbook

This is the living boot/start procedure for restoring the deployed FlowPilot application after SAP BTP trial-account cleanup or an intentional application stop. Update this section whenever a checkpoint adds, removes, renames, binds, or changes the startup order of a runtime dependency. Do not assume that commands from an earlier checkpoint still describe the deployed system.

**Last verified:** 2026-08-17, against Cloud Foundry organization `7d472741trial`, space `dev`, API endpoint `https://api.cf.us10-001.hana.ondemand.com`.

### Current deployment boundary

The live deployment is still the authentication-only application from MTA 0.1.3. The completed Checkpoint 2 backend exists in the repository but has not been deployed. Consequently, the live application has two Cloud Foundry runtime applications and does not yet depend on PostgreSQL at runtime.

### Daily startup procedure

1. Log in to Cloud Foundry using SSO:

   ```powershell
   cf login --sso -a https://api.cf.us10-001.hana.ondemand.com
   ```

2. Select and verify the expected organization and space:

   ```powershell
   cf target -o 7d472741trial -s dev
   cf target
   ```

3. Inspect application state before changing anything:

   ```powershell
   cf apps
   ```

4. Start the API first, followed by the AppRouter. These commands are safe to run when the applications are already started:

   ```powershell
   cf start flowpilot-api
   cf start flowpilot-approuter
   ```

5. Confirm that both applications show requested state `started` and process state `web:1/1`:

   ```powershell
   cf apps
   ```

6. Open the application and complete the real authentication callback:

   ```text
   https://7d472741trial-dev-flowpilot-approuter.cfapps.us10-001.hana.ondemand.com
   ```

The BTP cockpit alternative is **Subaccount -> Cloud Foundry -> Spaces -> dev -> Applications**. Start `flowpilot-api`, then `flowpilot-approuter`, and confirm that each has one running instance.

### Current resource startup matrix

| Resource                 | Current role                             | Daily startup action           | Reason or verification                                                                                                      |
| ------------------------ | ---------------------------------------- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------- |
| `flowpilot-api`          | Cloud Foundry API runtime                | `cf start flowpilot-api`       | Trial cleanup stops applications daily. Verify `web:1/1`.                                                                   |
| `flowpilot-approuter`    | Authenticated UI entry point and routing | `cf start flowpilot-approuter` | Trial cleanup stops applications daily. Verify `web:1/1`.                                                                   |
| `flowpilot-web`          | Build-time static web content            | None                           | It is packaged into the AppRouter and is not a separate Cloud Foundry runtime application.                                  |
| `flowpilot-auth`         | Managed XSUAA service                    | None                           | Managed service instances are not started with `cf start`; verify their broker operation with `cf services` when necessary. |
| `flowpilot-destination`  | Managed Destination service              | None                           | Managed service; no daily application-start command.                                                                        |
| `flowpilot-logs`         | Managed Application Logging service      | None                           | Managed service; no daily application-start command.                                                                        |
| `flowpilot-postgres`     | Retained PostgreSQL trial service        | None at the current checkpoint | It is healthy but unbound, and the live MTA 0.1.3 application does not use it yet.                                          |
| `demo-api`, `demo-iflow` | Unrelated Integration Suite resources    | None for FlowPilot             | They are not bound to the current FlowPilot applications and are not part of its boot sequence.                             |

Inspect managed-service provisioning state with:

```powershell
cf services
cf service flowpilot-postgres
```

`flowpilot-postgres` should currently report `create succeeded`. There is no `cf start flowpilot-postgres` operation: `cf start` controls Cloud Foundry applications, while PostgreSQL is broker-managed. Once a deployed checkpoint binds the API to PostgreSQL, this runbook must add database-readiness verification before accepting API health.

### Failure triage

If an application is requested as `started` but does not reach `web:1/1`, inspect its logs before redeploying:

```powershell
cf logs flowpilot-api --recent
cf logs flowpilot-approuter --recent
```

Then retry only the affected runtime application:

```powershell
cf restart flowpilot-api
cf restart flowpilot-approuter
```

Use this decision boundary:

- `stopped`: start the two runtime applications.
- `started` with `web:0/1`, `crashed`, or repeated restarts: collect recent logs and diagnose the dependency or startup failure.
- managed service present with a successful last operation: do not attempt an application-style start.
- managed service missing or showing a failed broker operation: treat this as service recovery, not normal daily startup, and do not recreate or delete it without reviewing data-loss and binding consequences.
- application missing: treat this as deployment recovery. Do not blindly deploy the latest repository state because it may contain a later checkpoint with additional configuration and secret requirements.
- trial account suspended or expired: restore or extend the trial account in the BTP cockpit; application start commands cannot repair an invalid account.

### Mandatory update rule for future checkpoints

At every deployment-affecting checkpoint, update this runbook in the same approved task and record:

1. The exact deployed MTA or application version and verification date.
2. Every runtime application and the required startup order.
3. Every required managed service, binding, route, destination, credential source, and external dependency.
4. Which services are manually startable, broker-managed, automatically resumed, or subject to lifecycle expiry.
5. The minimum health and smoke tests that prove the complete application is usable.
6. Failure-specific diagnostic commands and the safe recovery boundary.
7. Any newly discovered hurdle, unsuccessful diagnostic, corrected assumption, and final verified solution in the dated problem-solving audit trail.

Primary operational references: [SAP BTP trial accounts and free tier](https://help.sap.com/docs/btp/sap-business-technology-platform/trial-accounts-and-free-tier?locale=en-) and [Start, Stop, and Restart Applications](https://help.sap.com/docs/btp/sap-business-technology-platform/start-stop-and-restart-applications).

### 2026-08-17 — Trial-account daily startup requirements

- Objective: Establish a definitive daily recovery procedure for the currently deployed FlowPilot application without confusing Cloud Foundry applications with broker-managed services.
- Initial symptom: SAP BTP trial cleanup can stop runtime applications daily, and it was unclear whether the retained PostgreSQL trial instance also needed a manual start.
- Constraints and approval boundary: Inspect and document the current environment only; do not redeploy FlowPilot, change service bindings, expose credentials, or begin the next checkpoint.
- Evidence collected: SAP's current trial documentation states that applications stop automatically each day and require manual restart. Live Cloud Foundry inspection showed `flowpilot-api` and `flowpilot-approuter` at `web:1/1`; `flowpilot-postgres` reported `create succeeded` and no bound applications.

| Sequence | Hypothesis or question                                                          | Action or experiment                                                                                             | Result                                                                                                                 | Decision or next step                                                                                           |
| -------- | ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| 1        | Which FlowPilot resources are runtime applications that trial cleanup can stop? | Ran `cf apps` against the confirmed organization and space.                                                      | Exactly two FlowPilot runtime applications were present: `flowpilot-api` and `flowpilot-approuter`, both at `web:1/1`. | Put only these applications in the current daily `cf start` sequence.                                           |
| 2        | Does the web module require a separate start?                                   | Compared the live application list with the MTA module responsibilities already verified during deployment.      | No `flowpilot-web` runtime application exists; its output is packaged into the AppRouter.                              | Explicitly exclude `flowpilot-web` from startup commands.                                                       |
| 3        | Does PostgreSQL require a daily `cf start`?                                     | Ran `cf services` and `cf service flowpilot-postgres`; compared the resource type with CF application controls.  | PostgreSQL is a managed service with `create succeeded`, no bound apps, and no application-style start operation.      | Do not run or invent `cf start` for PostgreSQL; inspect broker state instead.                                   |
| 4        | Is PostgreSQL currently required for the user-visible deployed app?             | Compared the live MTA 0.1.3 deployment boundary with the completed but undeployed Checkpoint 2 repository state. | The live authentication-only application is not bound to PostgreSQL; the new private-chat backend is not deployed.     | Do not make PostgreSQL a current daily boot prerequisite; add readiness checks when the backend is deployed.    |
| 5        | What evidence distinguishes a successful start from requested state alone?      | Reviewed SAP's application operations guidance and the live `cf apps` process counts.                            | Requested state can be `started` while an instance is unhealthy; `web:1/1` is required for this deployment.            | Require both requested state and running process count, followed by a real browser authentication smoke test.   |
| 6        | Can the normal Markdown formatting check validate the runbook update?           | Tried global `npx prettier --check DevFlow.md`, then invoked the repository-installed Prettier directly.         | The global `npx` launcher referenced a missing module; the repository-local formatter wrote and validated the file.    | Treat the first error as a machine-level launcher fault and use the locked repository tool for reproducibility. |

- Root cause: There was no application-specific operational runbook distinguishing daily-stopped CF applications from managed services and build-only modules.
- Final change: Added the living FlowPilot startup runbook, current resource matrix, exact CLI and cockpit sequence, health criteria, failure boundaries, and mandatory checkpoint update rule.
- Verification: Current CF target, applications, bindings, and PostgreSQL broker status were inspected without mutation; the documented application names and states match the live `dev` space. Repository-local Prettier and `git diff --check` pass.
- Incorrect or incomplete assumptions corrected: A provisioned PostgreSQL trial service is not another Cloud Foundry application and cannot be started with `cf start`. A requested state of `started` is not by itself proof that an application instance is healthy.
- Reusable lessons: Generate startup procedures from the deployed topology rather than the repository's newest code. Separate daily application startup, managed-service inspection, and deployment recovery into distinct operational paths. Prefer the repository's locked formatter over a machine-global launcher.
- Remaining risks or follow-up: The runbook must change when Checkpoint 2 is deployed because the API will then require PostgreSQL and secure model-provider configuration. Trial-plan lifecycle expiry is separate from daily application stopping and requires monitoring and backup planning.

### 2026-08-17 — Checkpoint 3 SAP chat interface implementation

- Objective: Replace the custom authenticated status page with an accessible, responsive SAP Horizon chat interface that consumes the approved private-conversation API without deploying it to BTP.
- Initial symptom: The deployed-style frontend was a static custom-gradient page. It could verify `/api/me` but had no conversation navigation, message history, composer, provider-safe error states, AppRouter CSRF handling, or frontend interaction tests.
- Constraints and approval boundary: Implement Checkpoint 3 only. Use SAP UI5 Web Components for React and SAP theme variables, preserve complete-response messaging, keep model/provider selection server-side, do not configure secrets, and do not deploy or begin Checkpoint 4.
- Evidence collected: The approved milestone defined the UI states and API contract. Current UI5 documentation confirmed that `sap_horizon` is the default theme and that UI5 styling uses SAP CSS variables. Current AppRouter documentation confirmed that authenticated non-GET/HEAD routes require a fetched `X-CSRF-Token` by default.

| Sequence | Hypothesis or question                                                                       | Action or experiment                                                                                                                                                                                                                                               | Result                                                                                                                                                                                                                                                                                | Decision or next step                                                                                                                                                                                                           |
| -------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | Which current SAP component version and theme contract should the UI use?                    | Verified the current UI5 Web Components React package, UI5 theme configuration, and CSS-variable guidance before changing dependencies.                                                                                                                            | UI5 React 2.25.0 was current; Morning Horizon (`sap_horizon`) remained the built-in default; SAP global theme tokens support responsive custom layout without a custom color system.                                                                                                  | Pin compatible 2.25.x UI5 packages, set `sap_horizon` explicitly, and use SAP variables for every application-level color, border, shadow, font, and radius.                                                                    |
| 2        | Why did the normal npm commands not give a reproducible dependency result?                   | Tried the normal `npm install`/`npm ls` path, inspected the launcher, and then called the installed npm CLI through the known Node executable.                                                                                                                     | The machine launcher redirected to a missing roaming `npm-cli.js`; the explicit installed CLI completed dependency installation and reported zero vulnerabilities.                                                                                                                    | Record this as a workstation launcher fault and use the explicit installed CLI for verification rather than changing application dependencies to work around it.                                                                |
| 3        | Why was `ShellBar` unavailable after installing the React wrapper?                           | Inspected the installed dependency tree and the wrapper's peer metadata.                                                                                                                                                                                           | `@ui5/webcomponents-fiori` is an optional peer dependency even though `ShellBar` imports its implementation, so it remained unmet.                                                                                                                                                    | Add the matching Fiori package explicitly instead of relying on an optional transitive installation.                                                                                                                            |
| 4        | Will browser POST requests work through the existing secure AppRouter route?                 | Reviewed the AppRouter CSRF contract and expanded the typed API client to fetch, cache, send, refresh, and retry a CSRF token once.                                                                                                                                | The previous GET-only frontend would have received `403` on protected POST requests. Unit tests proved initial-token use, expired-token refresh, one retry, safe error parsing, and encoded conversation paths.                                                                       | Keep AppRouter CSRF enabled and own the token lifecycle in the frontend API boundary.                                                                                                                                           |
| 5        | Do the main create, load, send, keyboard, failure, and accessibility states work under test? | Added React Testing Library, jsdom, and axe-core tests, then ran the focused suite.                                                                                                                                                                                | Nine of ten tests initially passed. The failure came from querying one `Test User` string even though the accessible UI intentionally rendered it in several contexts.                                                                                                                | Change the assertion to accept multiple legitimate occurrences; do not remove useful user context to satisfy an over-specific test.                                                                                             |
| 6        | Can UI5 run without an external runtime asset dependency?                                    | Observed UI5's test warning about falling back to a CDN for locale data and imported the UI5 main and Fiori asset registries in the production entry point.                                                                                                        | The warning disappeared and the build emitted local theme and locale resources, avoiding a runtime CDN dependency.                                                                                                                                                                    | Keep UI5 assets self-hosted in the application package; revisit supported-locale pruning only as a measured optimization.                                                                                                       |
| 7        | Does the production bundle stay within Vite's default chunk recommendation?                  | Built the UI, then replaced root-barrel UI5 imports with supported per-component exports and rebuilt.                                                                                                                                                              | The build succeeded, but the self-contained UI5 runtime entry remained about 946 KB uncompressed and Vite retained its 500 KB advisory. The change reduced accidental import coupling but did not materially reduce the entry chunk.                                                  | Accept the warning for this checkpoint because the compressed entry is about 220 KB and behavior is correct; keep bundle analysis/code splitting as a documented performance follow-up rather than hiding the warning.          |
| 8        | Did a quiet production-build retry expose a code failure?                                    | Invoked Vite from the repository root with the web config to reduce noisy asset output, then reran it from `apps/web`.                                                                                                                                             | The root invocation could not find `index.html` because Vite resolves its entry from the working directory; the module-directory invocation succeeded and reproduced only the known size advisory.                                                                                    | Classify the first command as an invalid verification invocation, not a product defect; run module-relative build tools from their owning module.                                                                               |
| 9        | Does the browser rendering match the intended desktop layout?                                | Started a deterministic local mock API and Vite preview, opened the actual page, inspected its DOM, geometry, and screenshots. The first full-page screenshot was visually ambiguous, so viewport geometry and a normal screenshot were used to confirm placement. | The Horizon components rendered, but conditional omission of the error strip caused CSS Grid auto-placement to put the message region and composer in the wrong rows, leaving excess composer height.                                                                                 | Assign explicit grid rows to the header, optional error strip, message region, and composer; verify again in the real browser.                                                                                                  |
| 10       | Does the corrected layout remain usable on a narrow screen?                                  | Rechecked desktop geometry, applied a temporary 390 x 844 viewport, inspected panel bounds and horizontal overflow, and captured the mobile rendering.                                                                                                             | Desktop message/composer proportions were corrected. Mobile stacked navigation and chat without horizontal overflow, and UI5 preserved touch-sized actions and accessible labels.                                                                                                     | Accept both breakpoints and reset the temporary browser viewport after verification.                                                                                                                                            |
| 11       | Can real browser automation operate the UI5 shadow-DOM composer and account menu?            | First used the semantic Playwright label locator, then used the browser's visible-DOM interaction path when `fill` could not target the internal textarea. Sent a message with Ctrl+Enter and opened the ShellBar profile action.                                  | The semantic tree exposed the textbox, but the direct fill locator returned no match for the shadow input. The visible-DOM path successfully typed and submitted; the mock response appeared, the draft cleared, and the account dialog showed authenticated-user and tenant context. | Keep semantic assertions in automated tests and use a shadow-DOM-aware interaction surface for browser smoke tests; do not treat an accessibility-tree match as proof that every automation locator can mutate a web component. |
| 12       | Why did the first full-repository gate report missing API test and TypeScript executables?   | Inspected `apps/api/node_modules` after the earlier production packaging attempt and compared it with the MTA commands.                                                                                                                                            | The MTA build deliberately runs `npm prune --omit=dev`; because it operates in the source module directory, the local development binaries had been removed.                                                                                                                          | Restore the API module from its lockfile with `npm ci --include=dev` before rerunning development gates; treat this as build-workspace mutation, not an application failure.                                                    |
| 13       | Was the API production-build failure a TypeScript or bundling defect?                        | Ran the full production build in the restricted workspace, inspected the `esbuild` access error, and repeated the exact API build with approved access.                                                                                                            | The restricted run could not traverse the parent workspace packages used by the API bundle. The unchanged command passed with the required filesystem access.                                                                                                                         | Keep the successful authorized build as the production result and record the first failure as a local sandbox boundary.                                                                                                         |
| 14       | Why did the first strict MTA invocation fail before building a module?                       | Read the MBT error, located the installed GNU Make executable, and repeated the build with `C:\Program Files (x86)\GnuWin32\bin` added only to that process's `PATH`.                                                                                              | MBT was installed, but GNU Make was not discoverable from the normal shell path. The scoped path correction allowed the module build to start.                                                                                                                                        | Do not change application code for a workstation tool-discovery fault; use the known Make path for strict local packaging.                                                                                                      |
| 15       | Did the first Make-enabled retry finish after its command wrapper stopped returning output?  | Monitored the exact build process and archive timestamp instead of assuming success, then validated and removed only MBT's generated temporary directory and Makefile after the process ended without a new archive.                                               | The wrapper lost the remaining output and no new MTAR was produced, so success could not be claimed.                                                                                                                                                                                  | Rerun the same strict build in a captured session and require its exit code and archive generation as evidence.                                                                                                                 |
| 16       | Why did the captured strict build fail while reinstalling the web module?                    | Inspected the `EPERM unlink` path and checked the two Node processes started for the browser smoke test.                                                                                                                                                           | The local Vite preview still held Rolldown's native Windows DLL open, preventing `npm ci` from replacing it.                                                                                                                                                                          | Stop only the two preview/mock processes started for this checkpoint, validate and remove only failed MBT artifacts, and rerun from a clean captured session.                                                                   |
| 17       | Could the local development dependencies be restored normally after successful packaging?    | Ran the locked API development install after MBT's production prune, then repeated the unchanged install with approved npm-cache access when the restricted run failed.                                                                                            | The restricted run received `EPERM` while reading one file in the user npm cache; the approved retry installed 211 packages, audited 212, and found zero vulnerabilities.                                                                                                             | Leave the workspace development-ready and record the cache permission failure as an environment limitation rather than weakening the locked install.                                                                            |
| 18       | Does the final deployable artifact pass the real strict packaging gate?                      | Ran the captured MBT build with strict mode after releasing the preview lock and watched all module builds, audits, metadata generation, archive creation, and cleanup to completion.                                                                              | API, web, and AppRouter modules all built successfully. MBT generated `mta_archives/flowpilot_0.1.3.mtar` (34,223,698 bytes), returned exit code 0, and cleaned its temporary build files.                                                                                            | Accept the MTA as the Checkpoint 3 packaging proof, retain it locally, and do not deploy it before Checkpoint 4 approval.                                                                                                       |

- Root cause: The original frontend implemented authentication status only. Additional implementation hurdles came from a broken machine-global npm launcher, an optional Fiori peer package, missing AppRouter CSRF-token handling, external UI5 locale fallback, CSS Grid auto-placement around conditional content, differing automation behavior across UI5 shadow-DOM surfaces, workstation Make discovery, production pruning of source-tree development dependencies, a preview-held native DLL, and restricted cache/package access during local verification.
- Final change: Added the SAP Horizon `ShellBar`, authenticated account popover, private conversation navigation, message history, empty/loading/busy/error states, responsive composer, Ctrl/Command+Enter submission, a typed CSRF-aware API client, locally bundled UI5 assets, and frontend behavior/accessibility tests. Removed the custom gradients and hard-coded color system in favor of SAP theme variables.
- Verification: All four module type-checks pass. The full deterministic suite passes 29 tests: 6 model-adapter, 1 agent-core, 12 API, and 10 web tests; the optional local PostgreSQL test remains skipped because no local database URL was supplied, while Checkpoint 2's real BTP PostgreSQL isolation gate is already approved. API and web production builds pass. The strict MTA build produced `flowpilot_0.1.3.mtar` with exit code 0. The frontend tests cover create/send/failure behavior, CSRF lifecycle, keyboard submission, landmarks, and an axe scan with no serious or critical violations. Real local browser checks passed desktop rendering, a 390 px layout with no horizontal overflow, message submission, response rendering, draft clearing, and account-menu behavior. Repository formatting and whitespace checks pass after the audit update.
- Incorrect or incomplete assumptions corrected: Installing the React wrapper alone does not guarantee Fiori components are present. A GET-only API client is insufficient when authenticated AppRouter routes protect POST requests. UI component tests do not prove grid placement. A semantic locator that reads a custom element may still be unable to fill its shadow input. Per-component imports do not by themselves eliminate the size of a self-contained UI5 runtime and asset set. A successful module build can mutate source-tree `node_modules`, and a stopped-looking command wrapper does not prove that an MTA build succeeded. A native build error can be caused by a preview-process file lock rather than the dependency lockfile.
- Reusable lessons: Verify routing security requirements when a UI evolves from reads to writes. Treat UI framework peer dependencies as explicit deployment inputs. Bundle localization assets locally when runtime CDN access is undesirable. Combine component tests, automated accessibility checks, geometry inspection, and real responsive interaction. Record invalid verification commands separately from product defects. Require an MTA exit code plus a newly generated archive, stop owned preview processes before clean installs, and restore development dependencies after production pruning.
- Remaining risks or follow-up: Checkpoint 3 needs human review. The new UI and Checkpoint 2 backend remain undeployed; no Groq call or Credential Store retrieval has occurred. The UI5 entry chunk exceeds Vite's advisory threshold and should be profiled if measured startup performance is unacceptable. Checkpoint 4 must bind services, configure the provider credential securely, deploy a versioned MTA, update the startup runbook, and perform the real authenticated persistence smoke test.
- Approval outcome: Human approval was received on 2026-08-18. The UI remains intentionally undeployed while Checkpoint 3A establishes the environment-bootstrap and recovery process.

### 2026-08-18 — Checkpoint 3A step 1: environment-bootstrap contract

- Objective: Add a human-taught, repeatable environment-bootstrap checkpoint before the live chat deployment grows the BTP dependency surface.
- Initial question: Determine whether a replacement trial could be rebuilt from Git without manually recreating entitlements, Cloud Foundry configuration, service instances, role assignments, secrets, and deployment steps.
- Constraints and approval boundary: The human approved Checkpoint 3 and authorized Checkpoint 3A, but required every infrastructure step to be explained and performed incrementally. Step 1 is documentation only; it does not authorize Terraform initialization or apply, BTP changes, secret configuration, MTA deployment, or Checkpoint 4.
- Evidence collected: The repository already assigns application modules and bound services to the MTA, defines FlowPilot role collections in `xs-security.json`, and runs credential-free verification in GitHub Actions. SAP's BTP Terraform provider covers account-level entitlements, environment instances, and role assignments, while browser SSO remains the suitable interactive authentication path for a trial account.

| Sequence | Hypothesis or question                                                   | Action or experiment                                                                                                              | Result                                                                                                                                                                                              | Decision or next step                                                                                                                                                        |
| -------- | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | Can Terraform replace every deployment tool?                             | Compared the current `mta.yaml` and `xs-security.json` ownership with the SAP BTP and Cloud Foundry Terraform capabilities.       | Terraform could model overlapping Cloud Foundry resources, but doing so would create competing owners and discard the MTA's existing dependency graph.                                              | Terraform owns BTP account prerequisites and user assignments; the MTA through `cf deploy` continues to own applications, services, bindings, routes, and XSUAA definitions. |
| 2        | Does GitHub Actions eliminate the need for BTP or CF tooling?            | Compared the credential-free verification workflow with the authentication and commands required for provisioning and deployment. | GitHub Actions is an execution environment; it would still call Terraform and CF APIs and would require a non-human credential for unattended deployment.                                           | Keep GitHub Actions verification-only for the trial. Orchestrate trial recovery locally after browser SSO; revisit workload identity for an enterprise account.              |
| 3        | What must remain manual when a trial expires?                            | Mapped actions inside the account against actions required to create and authenticate the account itself.                         | Account activation, legal acceptance, human SSO/MFA, unavailable-plan choices, secure secret input, destructive approvals, and identity mapping cannot safely be inferred by repository automation. | List these actions explicitly rather than presenting the future bootstrap as falsely zero-touch.                                                                             |
| 4        | Can the learning requirement coexist with a future one-command recovery? | Split the work into explain, run, verify, record, and approve gates before adding each proven operation to the orchestrator.      | The individual commands remain visible and teachable, while the final wrapper becomes an audited composition of already-understood steps.                                                           | Complete one learning step per approval and disable live deployment in the Checkpoint 3A bootstrap.                                                                          |

- Root cause: The project had application-level MTA automation and CI verification but no declared owner for account-level prerequisites, no recovery orchestration, and no teaching protocol for rebuilding a disposable trial environment.
- Final change: Added the Checkpoint 3A scope, resource-ownership matrix, guided-learning gates, eleven-step implementation sequence, recovery inputs, success criteria, unavoidable manual actions, and data-portability warning. Marked Checkpoint 3 approved and added Checkpoint 3A to the milestone and progress tracker.
- Verification: Repository-local Prettier, `git diff --check`, and local Markdown-link resolution pass. The milestone ordering, approval states, and no-live-deployment boundary were inspected after formatting. No BTP, Cloud Foundry, GitHub Actions, Terraform, application runtime, service instance, role assignment, secret, or database state was changed.
- Incorrect or incomplete assumptions corrected: Terraform should not own every resource merely because a provider can model it. GitHub Actions runs deployment tools but does not replace them. A Git clone restores code, not secrets, database contents, or XSUAA identity continuity.
- Reusable lessons: Give every resource exactly one declarative owner. Separate account bootstrap from application deployment. Teach the manual operation before hiding it behind orchestration. Make unavoidable human gates and data migration explicit in disaster-recovery design.
- Remaining risks or follow-up: Step 2 must inventory the actual workstation tools and versions without installing anything. Terraform configuration, state backend choice, entitlement discovery, role-assignment timing, backup format, and the final bootstrap implementation remain unapproved future learning steps. The current live application remains the authentication-only deployment.

### 2026-08-18 — Checkpoint 3A step 2: workstation tool inventory

- Objective: Teach the responsibility of every required local tool and establish an evidence-based readiness list without installing, upgrading, authenticating, or mutating infrastructure.
- Initial symptom: Earlier work had proven that FlowPilot could build and deploy from this workstation, but tool discovery depended on fallback paths and did not distinguish missing software, tools absent from `PATH`, repository-local executables, user-level launchers, or restricted-terminal access failures.
- Constraints and approval boundary: Run version, path, repository requirement, and local CF plugin checks only. Missing or misconfigured tools are findings; Step 2 does not authorize a fix or installation.
- Evidence collected: The repository requires Node `24.x`, locks the MBT npm wrapper, and invokes MBT through an npm script. The current MTA plugin requires CF CLI v8. Previous strict builds proved that the repository MBT binary and the installed GNU Make executable can package FlowPilot.

| Sequence | Hypothesis or question                                                                       | Action or experiment                                                                                                                                                                     | Result                                                                                                                                                                                                                              | Decision or next step                                                                                                                                                               |
| -------- | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | Which required commands resolve through the current terminal?                                | Ran path and version checks for Node, npm, Git, BTP CLI, CF CLI, Terraform, MBT, and GNU Make, plus a local CF plugin listing.                                                           | Node and Git resolved cleanly. BTP CLI and Terraform were missing. CF, npm, global MBT, and Make needed further interpretation rather than accepting the first output at face value.                                                | Separate genuine absence from repository-local tooling, off-`PATH` installations, and sandbox access failures.                                                                      |
| 2        | Were CF CLI and MultiApps damaged because the first check printed local-config write errors? | Repeated only `cf version` and `cf plugins` with approved access to CF's existing user configuration directory.                                                                          | CF CLI `8.18.4` and MultiApps `3.11.1` returned cleanly. The earlier error came from the restricted terminal blocking CF's temporary local configuration file.                                                                      | Record CF and MultiApps as ready and classify the first result as an invalid sandbox-constrained health check.                                                                      |
| 3        | Is MBT actually missing because no global `mbt` command resolves?                            | First checked an assumed `node_modules/mbt/cli.js` path, then inspected the locked package's `bin` declaration and invoked the repository shim.                                          | The assumed path does not exist. The correct repository launcher exists under `node_modules/.bin`; the npm wrapper is `1.2.49`, while its installed native executable reports `1.2.47`.                                             | Treat MBT as ready and repository-managed. Report wrapper and native versions separately rather than requiring a global install.                                                    |
| 4        | Could the repository requirements be parsed with ordinary PowerShell JSON conversion?        | Parsed `package.json` and `package-lock.json` with `ConvertFrom-Json`. After the lockfile failed because it contains an empty-string property, repeated the read using `-AsHashtable`.   | The first lockfile inspection command was invalid for this legal JSON shape. Hash-table parsing returned Node `24.x`, lockfile version `3`, and MBT wrapper `1.2.49`.                                                               | Preserve the lockfile and use a parser mode that supports all valid property names. Do not misclassify an inspection-tool limitation as corrupt project data.                       |
| 5        | Does the npm `MODULE_NOT_FOUND` result prove that the workstation npm launcher is broken?    | Inspected command resolution, the configured roaming prefix, and both npm installations. Because the redirected files existed, repeated `npm --version` with approved user-level access. | Normal npm works and reports `11.11.1`; the base Node installation contains npm `11.6.2`. The earlier failure was caused by restricted access to the user npm tree, not a broken workstation launcher.                              | Correct the prior assumption, keep npm ready, and let restricted automation use an explicitly accessible CLI without altering the user's valid npm configuration.                   |
| 6        | Is GNU Make unavailable or only undiscoverable?                                              | Checked normal command resolution and the exact executable previously used by the strict MTA build.                                                                                      | GNU Make `3.81` exists at `C:\Program Files (x86)\GnuWin32\bin\make.exe` but is not on `PATH`; it has already built FlowPilot successfully.                                                                                         | Record it as ready with a discovery issue. The future orchestrator may resolve a configured path but must not silently edit global `PATH`.                                          |
| 7        | Which installations are genuinely required before the next lessons?                          | Compared final tool evidence with the Step 3 SSO/discovery needs and the later Terraform lesson.                                                                                         | SAP BTP CLI and Terraform are the only missing required tools. All other required capabilities are usable in their documented resolution context.                                                                                   | Stop before installation. Present purpose, source, version pin, install command, verification, and removal implications for separate human approval.                                |
| 8        | Should both missing tools be installed together?                                             | Reviewed the current official SAP BTP CLI and HashiCorp Terraform installation guidance and mapped each tool to the learning sequence.                                                   | SAP BTP CLI is required for Step 3, while Terraform is not needed until Step 4. SAP offers an official Windows installer/WinGet path; HashiCorp currently publishes stable Terraform `1.15.8` Windows AMD64 binaries and checksums. | Install and verify BTP CLI in its own approved lesson. Defer the pinned, checksum-verified Terraform installation so the human learns why it is needed immediately before using it. |

- Root cause: The workstation had two genuinely missing tools, but initial command resolution conflated them with a repository-local MBT, an off-`PATH` Make executable, and user-configuration access denied by the restricted terminal.
- Final change: Added the dated workstation compatibility matrix, recorded exact versions and resolution contexts, and marked the milestone's read-only tool inventory complete. No executable, package, path, login session, GitHub workflow, or platform resource was changed.
- Verification: Node `24.12.0` satisfies the repository's `24.x` engine. Normal npm `11.11.1`, Git `2.52.0.windows.1`, CF CLI `8.18.4`, MultiApps `3.11.1`, repository MBT wrapper `1.2.49`/native `1.2.47`, and GNU Make `3.81` were directly queried. BTP CLI and Terraform did not resolve. Repository-local Prettier and `git diff --check` pass after the inventory update.
- Incorrect or incomplete assumptions corrected: A command missing from global `PATH` is not necessarily absent. An npm wrapper package and its downloaded native binary can expose different versions. A sandboxed `MODULE_NOT_FOUND` or config-write error is not proof that the user's installation is broken. PowerShell's default JSON object conversion does not accept every property shape supported by JSON.
- Reusable lessons: Inventory path, version, invocation context, and ownership separately. Retry access-denied diagnostics in the correct permission context before recommending repairs. Prefer repository-locked build tools where possible, and record native binaries separately when wrappers download them.
- Remaining risks or follow-up: SAP BTP CLI and Terraform still need separately approved installation lessons. The official sources are identified, but no binary or installer has been downloaded. Step 3 SSO and target discovery cannot begin until BTP CLI is available. Terraform remains deferred until Step 4. No current BTP or CF target was inspected during Step 2.

### 2026-08-18 — Checkpoint 3A step 2A: SAP BTP CLI installation

- Objective: Teach, install, and verify the SAP BTP CLI from SAP's official distribution without logging in or changing any BTP account state.
- Initial symptom: Step 2 proved that `btp` did not resolve, blocking the upcoming browser-SSO and account-discovery lesson.
- Constraints and approval boundary: Install only the BTP CLI at user scope. Review the official installer before execution, verify the downloaded artifact, explain installation and removal, and leave authentication for Step 3. Do not install Terraform or change BTP, CF, GitHub, application, service, role, secret, or database state.
- Evidence collected: SAP's current documentation offers an official PowerShell installer, WinGet, or manual Windows AMD64 archive. The official installer uses `%LOCALAPPDATA%\Programs\btpcli`, adds that directory to user `PATH`, downloads from `tools.hana.ondemand.com`, checks a published SHA-1, and links installation to SAP's Developer License 3.2.

| Sequence | Hypothesis or question                                                                                      | Action or experiment                                                                                                                                                                                                                                | Result                                                                                                                                                                                                                                                                         | Decision or next step                                                                                                                                                        |
| -------- | ----------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | Is SAP's documented remote installer suitable to execute without inspection?                                | Downloaded the official `btpcli-install.ps1` to a temporary review directory, recorded its SHA-256, and inspected its download, checksum, extraction, install-path, `PATH`, cleanup, and license-related lines.                                     | The 5,587-byte script installs the latest client to the documented user-local directory and updates user `PATH`. Its reviewed SHA-256 was `6CB6292D779EF6857525115937EAB3AEF4BD86B46A138C6F6370B9BC3714E3AC`.                                                                  | Do not pipe the unreviewed network response directly into PowerShell; use the reviewed behavior with explicit verification gates.                                            |
| 2        | Does the official installer fail closed if checksum verification fails?                                     | Read the complete download and checksum branch before running it.                                                                                                                                                                                   | The script writes a checksum error when values differ but has no explicit terminating statement before its extraction and move operations.                                                                                                                                     | Implement the same official download manually and throw before extraction on any mismatch. Record this as the reason the documented one-liner was not executed.              |
| 3        | Could the installation overwrite an existing user tool or duplicate a path entry?                           | Inspected the documented install directory and exact user `PATH` entries before downloading the archive.                                                                                                                                            | `C:\Users\saura\AppData\Local\Programs\btpcli` did not exist, no `btp.exe` was present there, and the directory was not already in user `PATH`.                                                                                                                                | Proceed with a new user-local installation and refuse unexpected pre-existing targets.                                                                                       |
| 4        | Is the downloaded archive the artifact SAP published, and is it safe to extract?                            | Downloaded the official latest Windows AMD64 archive and `.sha1`, required an exact checksum match, listed entries, rejected absolute/traversal/unexpected paths, extracted into a temporary directory, and ran `--info` from there.                | Published SHA-1 `85f193ea89a5667b68eb8cf71a17d765ab507aa3` matched. Recorded archive SHA-256 was `3EDFBF6C9A0069D1A265CA3E061497ED605CCB6F400CAE3D0708488BDCBB396C`. The only entry was `windows-amd64/btp.exe`; it reported client `2.106.1`, server `2.116.2`, and no login. | Accept the temporary binary for user-local installation.                                                                                                                     |
| 5        | Does the persistent installation exactly match the verified binary and resolve predictably?                 | Created the documented user-local directory, copied only the verified executable, compared SHA-256 values, added the containing directory once to user `PATH`, and queried the installed executable.                                                | Installed binary SHA-256 `F3C1BA51225382E89B3F9B78EE74817E5BDF21D779A1A29D77E9448A5B81B0DD` matched its verified temporary source. `btp` resolved to the intended path and still reported not logged in.                                                                       | Mark BTP CLI ready. Open a new user terminal if an existing terminal has not refreshed its inherited `PATH`.                                                                 |
| 6        | Can installation artifacts be cleaned without risking unrelated temporary data?                             | Resolved both exact temporary directories, verified each remained beneath the system temp root with the expected leaf name, removed only those directories, and rechecked the installed executable.                                                 | Both temporary directories were removed and the installed CLI remained present.                                                                                                                                                                                                | Retain only the installed binary, user `PATH` entry, and non-secret audit hashes.                                                                                            |
| 7        | Did the final restricted-terminal verification prove that the installed CLI or persisted `PATH` was broken? | Tried to execute the user-local binary and read user `PATH` after documentation formatting, then repeated the exact read-only checks with normal user-level access after the restricted run returned access denied and a false missing-path result. | The authorized check reproduced the installed SHA-256, confirmed the exact user `PATH` entry, reported client `2.106.1`/server `2.116.2`, and remained logged out.                                                                                                             | Classify the first final check as sandbox-constrained, retain the installation unchanged, and require the appropriate access context for user-local executable verification. |

- Root cause: SAP BTP CLI was genuinely absent from the workstation; no application or account defect was involved.
- Final change: Installed SAP BTP CLI client `2.106.1` at user scope from a checksum-verified official archive, added its containing directory once to user `PATH`, removed temporary artifacts, and left the client logged out.
- Verification: The installed binary matched the verified temporary binary, resolved from the intended path, retained SHA-256 `F3C1BA51225382E89B3F9B78EE74817E5BDF21D779A1A29D77E9448A5B81B0DD`, reported client `2.106.1` and server `2.116.2`, confirmed the persisted user `PATH` entry, and showed no active login. Repository-local Prettier and `git diff --check` pass after the installation record update.
- Incorrect or incomplete assumptions corrected: An official installation one-liner is not a reason to skip script inspection. Printing a checksum error is not equivalent to terminating before extraction. The `latest` archive name must be resolved to and recorded as an actual client version for a reproducible audit.
- Reusable lessons: Inspect remote installers before execution, prefer fail-closed checksum handling, validate archive paths before extraction, compare source and installed binary hashes, use user-local installations when administrator rights are unnecessary, and document exact rollback ownership.
- Remaining risks or follow-up: Existing terminal windows may need to be reopened to inherit the updated user `PATH`. The client is intentionally logged out. Step 3 must teach `btp login --sso`, verify the target without exposing tokens, compare BTP and CF targets, and stop before writing an environment profile. Terraform remains uninstalled and deferred.

### 2026-08-18 — Checkpoint 3A step 3: BTP/CF SSO and environment discovery

- Objective: Authenticate the installed SAP BTP CLI, confirm the existing Cloud Foundry session, discover the current trial topology without changing it, and save only non-secret target values in a Git-ignored local profile.
- Initial symptom: The SAP BTP CLI was installed but logged out, the CF target had not been compared with the BTP account hierarchy in this checkpoint, and no reusable local environment-profile schema existed.
- Constraints and approval boundary: The human temporarily authorized autonomous execution and deferred detailed teaching until project completion. Authentication sessions and read-only discovery were permitted; tokens, provider credentials, service keys, and personal user identifiers must not enter Git. Starting applications, changing account resources, Terraform apply, and live deployment remained out of scope.
- Evidence collected: Browser SSO authenticated the BTP CLI to the expected trial global account. BTP discovery returned exactly one `trial` subaccount in `us10`. CF remained authenticated to API `https://api.cf.us10-001.hana.ondemand.com`, organization `7d472741trial`, and space `dev`. The two deployed applications were stopped by normal trial cleanup; all retained managed services reported successful broker operations.

| Sequence | Hypothesis or question                                                                | Action or experiment                                                                                                                                                             | Result                                                                                                                                                                                               | Decision or next step                                                                                                                    |
| -------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | Can BTP authentication complete through browser SSO without copying tokens into chat? | Started `btp login --sso manual`, opened the one-time SAP confirmation URL in the existing authenticated browser context, confirmed the CLI login, and returned to the terminal. | SAP reported successful identity-provider authentication; the terminal completed login and targeted the expected trial global account. No token, password, or session file was printed or committed. | Reuse browser SSO for human-operated trial recovery and retain MFA or credential entry as a human handoff when required.                 |
| 2        | Does the BTP CLI target match the existing Cloud Foundry environment?                 | Queried the BTP target and attempted subaccount discovery together with `cf target`, `cf apps`, and `cf services`.                                                               | BTP targeted the expected global account. CF targeted `us10-001`, organization `7d472741trial`, space `dev`; application and managed-service names matched the existing FlowPilot landscape.         | Treat the targets as aligned, but retain the exact subaccount UUID and region check before later Terraform work.                         |
| 3        | Can subaccount discovery be consumed as JSON using a conventional `--format` option?  | Ran `btp list accounts/subaccount --format json`.                                                                                                                                | Client `2.106.1` rejected `--format` as unknown and printed supported syntax. No account state changed.                                                                                              | Rerun the supported command without the option; do not build automation around an unverified output flag.                                |
| 4        | What is the supported account discovery result?                                       | Ran `btp list accounts/subaccount` and inspected `btp get accounts/subaccount --help`.                                                                                           | Exactly one healthy `trial` subaccount was returned in region `us10`, with stable account identifiers.                                                                                               | Store identifiers locally in an ignored profile; commit only a placeholder schema.                                                       |
| 5        | Should stopped trial applications be started as part of environment discovery?        | Compared `cf apps` with the Checkpoint 3A boundary and the current startup runbook.                                                                                              | Both applications were stopped, while services were healthy. Starting them would be a runtime mutation unrelated to target discovery.                                                                | Leave them stopped. Daily startup remains an explicit operator action; pending chat code remains undeployed until Checkpoint 4 approval. |
| 6        | Can the target be recorded without publishing account UUIDs or credentials?           | Added `config/environments/btp.local.json` to `.gitignore`, wrote the discovered UUIDs only to that local file, and committed a placeholder `btp.example.json` schema.           | Git sees the example and ignore rule but not the real profile. The profile contains targeting metadata only and no credentials, tokens, service keys, model keys, or Terraform state.                | Use the ignored profile as the single local input for later Terraform and bootstrap validation.                                          |
| 7        | Can the normal Prettier command validate every changed file including `.gitignore`?   | Included `.gitignore` in the repository-local Prettier write and check command, then reran formatting on only supported JSON and Markdown files plus `git diff --check`.         | Prettier could not infer a parser for `.gitignore`; the supported files passed, and Git's whitespace validation passed the complete diff.                                                            | Do not force a parser or alter the ignore file mechanically. Use Prettier for supported formats and `git diff --check` for `.gitignore`. |

- Root cause: Tool installation and authentication were complete in separate stages, but no verified bridge connected the BTP account hierarchy to the CF deployment target or to a safe, repeatable local input contract.
- Final change: Authenticated BTP through browser SSO, verified BTP and CF targets, captured one healthy trial subaccount, added an ignored local profile and committed placeholder schema, and documented the temporary fast-track learning exception.
- Verification: BTP and CF read-only commands returned the expected account, subaccount, region, API, organization, and space. Both JSON profiles parse, `git check-ignore` confirms the real profile is excluded while the example remains trackable, supported files pass repository-local Prettier, and `git diff --check` passes. No BTP resource or CF application was changed.
- Incorrect or incomplete assumptions corrected: `btp list accounts/subaccount` does not support a generic `--format json` option in client `2.106.1`. A stopped trial application is not evidence of an account-targeting problem and should not be started during read-only discovery.
- Reusable lessons: Separate authentication from resource mutation. Verify account and runtime targets together. Never assume a CLI offers a conventional machine-output flag. Commit schemas, not environment identities or credentials. Keep deferred teaching tied to the real audit evidence rather than discarding the learning phase.
- Remaining risks or follow-up: Terraform is still uninstalled. Before Terraform can initialize, install and checksum-verify a pinned client, define state and provider-authentication boundaries, and validate that the ignored profile is structurally correct. The BTP login remains a human SSO session and will expire. The detailed lesson is deferred to the project-close walkthrough.

### 2026-08-18 — Checkpoint 3A step 4A: Terraform CLI installation

- Objective: Install a pinned Terraform CLI from HashiCorp's official release channel with independent signature, checksum, archive, executable-signature, version, PATH, and cleanup verification before any provider initialization.
- Initial symptom: Terraform was the last missing workstation prerequisite and no version-resolved, verified binary was available to create or validate the BTP infrastructure configuration.
- Constraints and approval boundary: The temporary autonomous fast-track permits this reversible user-local tool installation. It does not authorize `terraform init`, provider authentication, Terraform state creation, plan, apply, account changes, or live application deployment. Existing installations must not be overwritten.
- Evidence collected: HashiCorp's current install page identified Terraform `1.15.8` for Windows AMD64. HashiCorp's security guidance identified the release-checksum signing-key fingerprint and required signature-plus-checksum verification. The final binary also passed Windows Authenticode validation as HashiCorp, Inc.

| Sequence | Hypothesis or question                                                             | Action or experiment                                                                                                                                                                                                                           | Result                                                                                                                                                                                                           | Decision or next step                                                                                                                          |
| -------- | ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | Is `1.15.8` still the current supported Windows AMD64 release?                     | Checked HashiCorp's current official install and archive-verification documentation immediately before download.                                                                                                                               | The install page listed `1.15.8` for Windows AMD64 and linked its SHA-256 checksums; security guidance required validating the signed checksum file and archive hash.                                            | Pin `1.15.8`; do not install an unversioned package-manager result.                                                                            |
| 2        | Can the official release be downloaded with PowerShell's web client?               | Created a validated unique temporary directory and attempted to download the archive, checksums, signature, and public key with `Invoke-WebRequest` inside fail-closed cleanup.                                                                | The first request failed because the remote host forcibly closed the TLS transport connection. The `finally` block removed the temporary directory; the install target and user `PATH` remained untouched.       | Retry the same official URLs with `curl --retry` rather than weakening TLS or skipping verification.                                           |
| 3        | Can transient connection resets be recovered without accepting a partial artifact? | Used `curl --fail --location --retry 3 --retry-all-errors` for each required artifact, then required every later cryptographic and structural check to pass.                                                                                   | Three connection-reset messages occurred, after which retries completed all downloads. A partial or corrupted archive could not pass the PGP-backed checksum comparison.                                         | Continue only through the complete verification chain; record the network instability rather than treating retry output as an install failure. |
| 4        | Did HashiCorp sign the checksum file and does the archive match it?                | Imported the official public key into a temporary GPG home, required fingerprint `C874011F0AB405110D02105534365D9472D7468F`, verified the detached checksum signature, selected exactly one Windows AMD64 checksum line, and compared SHA-256. | The signature was good and the archive matched SHA-256 `2FF41D2129AFB1982733C132C61A8D6EF038F879F3AEEDE7FC28B8B8B24ACF02`.                                                                                       | Accept the archive for structural inspection.                                                                                                  |
| 5        | Is the archive safe to extract and is the executable independently signed?         | Rejected rooted, traversal, and unexpected entries; allowed only `LICENSE.txt` and `terraform.exe`; extracted to the validated temporary directory; checked Windows Authenticode status and signer.                                            | The archive contained exactly the two allowed files. `terraform.exe` had a valid Windows signature from HashiCorp, Inc., with certificate thumbprint `65B9DA802B1273FB147374C3C122DB21ABECDFC4`.                 | Accept the executable for user-local installation.                                                                                             |
| 6        | Can installation avoid overwriting tools and duplicate PATH entries?               | Refused any existing target directory, copied only the verified executable to `C:\Users\saura\AppData\Local\Programs\terraform`, compared source and installed hashes, and added the containing directory only when absent from user `PATH`.   | The installed executable retained SHA-256 `6EB0A1CB89344C97CCF2928DDC2D7A6CB71A1837B7ECCCFD5991466B6D751E03`; one exact user `PATH` entry exists.                                                                | Keep the user-local installation; existing terminals may need reopening to inherit the new path.                                               |
| 7        | Did the installation alter Terraform state or leave verification artifacts?        | Queried the installed binary with `terraform version -json`, checked provider selections, revalidated Authenticode and SHA-256, counted exact user-path matches, and searched the system temporary directory for the installer prefix.         | Terraform reported `1.15.8`, `windows_amd64`, no provider selections, and not outdated. Signature and hash remained valid, exactly one path entry existed, and zero temporary installation directories remained. | Mark the CLI ready. Begin repository configuration only as the next separately auditable step.                                                 |

- Root cause: Terraform was intentionally deferred until the infrastructure lesson, so no verified CLI existed. The only installation hurdle was transient transport resets from HashiCorp's release host.
- Final change: Installed Terraform `1.15.8` at user scope from HashiCorp's PGP-signed checksum set, added its directory once to user `PATH`, and removed all temporary verification artifacts.
- Verification: Release key fingerprint, detached signature, archive SHA-256, archive paths, Authenticode status, installed executable SHA-256, reported version/platform, exact PATH cardinality, empty provider selection, and temporary-directory cleanup all passed.
- Incorrect or incomplete assumptions corrected: A TLS connection reset does not justify using an alternate unverified source or omitting signature checks. A downloaded archive checksum alone is weaker than validating that HashiCorp signed the checksum file. A CLI installation does not imply provider initialization or state creation.
- Reusable lessons: Pin tool versions, verify publisher identity and content integrity independently, use an isolated GPG home, validate archives before extraction, refuse silent overwrite, make PATH updates idempotent, and retry transport failures only while retaining the full fail-closed verification chain.
- Remaining risks or follow-up: Existing terminal windows may need reopening for ordinary `terraform` resolution. The SAP BTP provider version, local-state ignore rules, authentication method, configuration ownership, and first initialization still need to be designed and reviewed in Step 4B. No Terraform state exists yet.

### 2026-08-18 — Checkpoint 3A steps 4B–6: Terraform configuration and account-prerequisite plans

- Objective: Add a versioned SAP BTP Terraform boundary, initialize a pinned provider, validate the current target and required entitlements, prove a guarded recovery/import path, and determine whether any current account-level apply is necessary.
- Initial symptom: Terraform was installed but the repository had no provider configuration, state boundary, input schema, dependency lockfile, entitlement contract, environment validation, or reproducible plan.
- Constraints and approval boundary: The autonomous fast-track permits repository edits, provider download, read-only refresh, and plan generation. SSO remains a human handoff when browser automation is unavailable. No Terraform apply, state import, entitlement mutation, environment creation, application start, MTA deployment, or secret entry was authorized.
- Evidence collected: SAP/btp provider `1.25.0` supports local BTP CLI session reuse, entitlement/environment data sources, and identity-based entitlement imports. The current trial exposes every FlowPilot prerequisite and one healthy Cloud Foundry environment.

| Sequence | Hypothesis or question                                                                    | Action or experiment                                                                                                                                                                                                                                           | Result                                                                                                                                                                                                                                                                 | Decision or next step                                                                                                                                                                                                                |
| -------- | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1        | Did stopping the earlier chat leave a process or partial repository mutation?             | Checked Git status, recent commits, and active `btp`, `cf`, and `terraform` processes immediately after the user interrupted an unexpectedly long query.                                                                                                       | The worktree was clean at commit `2ae9c6d`, no relevant process remained, and no partial Terraform files existed.                                                                                                                                                      | Resume from configuration rather than repeating completed installation or authentication work.                                                                                                                                       |
| 2        | Why was the user left without an update during the previous BTP environment-detail query? | Reviewed the tool result and execution behavior after recovery. The nonessential `btp get accounts/environment-instance` call stayed inside the wrapper for roughly twenty minutes and eventually returned only after its SSO session expired.                 | The query was unnecessary because the earlier list already supplied environment ID, type, state, and landscape. The assistant had not imposed an effective visible stop condition and the user had to kill the chat.                                                   | Add a permanent DevFlow rule: bound routine external waits, report delay immediately, and cancel or bypass nonessential diagnostics. Use short interactive sessions for operations that may not honor the wrapper's requested yield. |
| 3        | Can target identifiers be extended without publishing the real account?                   | Added environment-instance ID and landscape to the ignored local JSON profile and placeholders to the committed example. Expanded `.gitignore` for Terraform caches, state, plans, variable files, crashes, overrides, and CLI configuration.                  | The real profile and all sensitive/state-bearing Terraform artifacts are ignored; the example schema and lockfile remain trackable.                                                                                                                                    | Use the ignored profile as the only local target input and never write credentials into variables or state intentionally.                                                                                                            |
| 4        | What provider and authentication flow fit an interactive trial?                           | Pinned Terraform `1.15.x` and SAP/btp `1.25.0`; configured only `globalaccount`; selected `USE_BTPCLI_SESSION=true` based on current provider documentation, which deprecates its older direct SSO flag and excludes CLI-session reuse from CI/CD.             | Configuration contains no user name, password, token, assertion, or certificate. GitHub Actions cannot use this personal interactive session.                                                                                                                          | Reuse `btp login --sso` locally; retain credential-free CI for trial and document future technical-identity options separately.                                                                                                      |
| 5        | Can the user-local Terraform binary be checked inside the restricted workspace process?   | Ran format checks in the default sandbox, then repeated the exact command with authorized user-level execution after access was denied.                                                                                                                        | The first failure was a sandbox denial against the installed executable, not invalid HCL. The authorized formatter completed; its wrapper was slower than expected, so progress was reported after control returned.                                                   | Execute verified user-local tools with the required access context and keep their cloud operations independently bounded.                                                                                                            |
| 6        | Can provider initialization survive the workstation's recurring registry resets?          | Ran `terraform init -backend=false -input=false`; after the first registry IPv6 connection reset, made one bounded retry.                                                                                                                                      | The retry installed SAP/btp `1.25.0`, reported its signing key, and created `.terraform.lock.hcl`. No backend or state was created.                                                                                                                                    | Commit the provider lockfile; do not loop indefinitely or substitute an unverified provider source.                                                                                                                                  |
| 7        | Can the configuration prove prerequisites without owning or changing them by default?     | Added subaccount, entitlement, and environment data sources; postconditions for identity/state; a missing-entitlement check; non-secret summary outputs; and `manage_entitlements=false` as the default.                                                       | Provider-aware `terraform validate` passed. The live default plan found all seven prerequisites, healthy BTP and CF targets, and proposed only local outputs.                                                                                                          | Keep discovery-only behavior as the default and require a separate reviewed manage-mode plan.                                                                                                                                        |
| 8        | Can the expired SSO session be refreshed without exposing credentials?                    | Started manual BTP CLI SSO. The browser-control client failed during setup because its runtime rejected an internal `node:process` import, so the assistant immediately supplied the one-time SAP confirmation link and continued local checks.                | The human completed the SAP confirmation, the CLI reported authentication success, and no password, token, session file, or authorization code was committed or copied into documentation.                                                                             | Treat browser-control setup failure as a manual SSO handoff, not a reason to wait silently or bypass authentication.                                                                                                                 |
| 9        | Why did the first Terraform plan exit before contacting BTP?                              | Invoked plan flags directly through PowerShell, received `Too many command line arguments`, then passed the same tokens through an explicit argument array.                                                                                                    | The corrected invocation reached all data sources and completed. The execution wrapper itself did not yield at the requested 30 seconds and returned control much later; once control returned, the assistant reported status and one short poll delivered the result. | Standardize Windows native CLI arguments as arrays and prefer a TTY/polling session for Terraform network operations so progress remains visible.                                                                                    |
| 10       | What does the current prerequisite plan actually propose?                                 | Saved the corrected default plan, parsed its JSON without printing the full account payload, counted managed changes, checked missing entitlements and state existence, then deleted only the validated plan path.                                             | Zero managed-resource changes, zero missing entitlements, healthy trial/CF targets, no state, and output-only differences. Categories were Platform `4`, two quota services at `1`, and four elastic services at provider defaults.                                    | Do not apply: the current account already satisfies the prerequisite contract, and saving outputs alone does not justify creating local state.                                                                                       |
| 11       | Can future recovery safely distinguish existing grants from missing ones?                 | Added opt-in entitlement resources, identity-based import blocks driven by an explicit existing-key set, exact quota amounts for quota/platform plans, and `prevent_destroy=true`. The first patch failed because one formatter-aligned example line differed. | Patch verification failed before changing files. After inspecting exact text, the corrected patch applied; formatter and provider-aware validation passed.                                                                                                             | Keep patching fail-closed. Generate and review the existing-key set from discovery; never guess it.                                                                                                                                  |
| 12       | Does guarded manage mode preview state adoption without cloud mutation?                   | Ran a TTY-backed saved plan with all seven discovered keys and `manage_entitlements=true`, reported progress at bounded polls, parsed plan JSON, and removed the temporary plan without applying it.                                                           | Terraform reported **7 imports, 0 adds, 0 updates, 0 deletes**. JSON counts matched; no state file was created and the ignored plan was removed.                                                                                                                       | Accept the guarded import/create design. A future missing key may become a reviewed creation, but apply remains an explicit human gate.                                                                                              |

- Root cause: The repository lacked an infrastructure-as-code boundary, while the main operational hurdle was unbounded or poorly yielding external command execution rather than a BTP configuration defect.
- Final change: Added Terraform `1.15.8` pinning, SAP/btp `1.25.0` locking, ignored local input/state boundaries, target and entitlement validation, discovery-only defaults, opt-in import/create semantics, prevent-destroy protection, and documented local BTP CLI session authentication.
- Verification: Terraform formatting and provider-aware validation pass. Live discovery found all seven entitlements and healthy BTP/CF targets. The default plan had zero managed changes and zero missing prerequisites. Manage-mode preview had seven imports and zero adds/updates/deletes. Both saved plans were parsed, kept out of Git, and removed; no Terraform state exists and no apply ran.
- Incorrect or incomplete assumptions corrected: A nonessential read-only CLI query is not harmless if it can monopolize the collaboration loop. `yield_time_ms` is not always an effective wall-clock guarantee. Direct PowerShell native arguments are not always preserved as intended. BTP CLI session reuse is local-only and the older provider SSO flag is deprecated. Existing trial grants must be imported before Terraform can manage them safely.
- Reusable lessons: Put external commands behind observable time bounds and use interactive polling when wrappers yield poorly. Separate discovery, plan, and apply. Commit provider locks but never state. Use explicit import inventories for pre-existing resources, fail closed on unknown keys, protect shared entitlements from destroy, and skip apply when a plan proves no account mutation is required.
- Remaining risks or follow-up: The Terraform state backend is intentionally local and absent; a shared encrypted backend is required before multi-operator enterprise use. Missing-entitlement creation is structurally configured but cannot be exercised without a genuinely missing reviewed prerequisite. Subaccount and Cloud Foundry environment creation remain deliberate manual/design gates for trial recovery. The next Checkpoint 3A step is a fresh strict MTA build and archive inspection without deployment.

### 2026-08-18 — Checkpoint 3A step 7: reproducible MTA build and inspection

- Objective: Prove that the current repository can produce a complete BTP Cloud Foundry MTA with locked dependencies, inspect the deployable payload, and keep live deployment outside Checkpoint 3A.
- Initial symptom: Earlier checkpoints had produced an MTAR, but environment recovery needed a new, independently observed strict build and an explicit archive-content and secret-exclusion check.
- Constraints and approval boundary: Local dependency restoration, compilation, packaging, hashing, and read-only archive inspection were allowed. `cf deploy`, application start, service mutation, provider-secret entry, and Terraform apply remained prohibited.
- Evidence collected: Repository MBT wrapper `1.2.49` invoked native MBT `1.2.47` in strict mode with GNU Make `3.81` made discoverable only for the build process. Every dependency install reported zero vulnerabilities.

| Sequence | Hypothesis or question                                                                          | Action or experiment                                                                                                                                                                    | Result                                                                                                                                                                                                                                                     | Decision or next step                                                                                                                      |
| -------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 1        | Can the current source build all runtime modules with locked dependencies?                      | Ran `npm run mta:build`, which invokes `mbt build --strict=true`, in one observable TTY session with the documented GNU Make path scoped to that process.                               | Model adapters and agent core installed and compiled; the API installed, compiled, and pruned production dependencies; the UI installed and built 701 Vite modules; the AppRouter installed production dependencies; every npm audit reported zero issues. | Keep lockfile-based installation and module ordering as the packaging gate.                                                                |
| 2        | Did a quiet packaging interval mean that MBT was stuck?                                         | Kept the original build session, checked that MBT/Make still had activity, and used short polls instead of starting another build.                                                      | API compression completed normally. The UI dependency install then took roughly three minutes while emitting progress, and the build continued.                                                                                                            | Treat compression and large locked installs as expected slow phases, but report them and retain one bounded, observable process.           |
| 3        | Did an invalid polling helper or oversized output invalidate the build?                         | A JavaScript polling wrapper had a syntax error, and a later chunk containing the Vite asset report exceeded the response budget. Polled the same session again with a smaller bound.   | Neither orchestration error reached the child process. The same MBT process completed, returned exit code `0`, generated metadata and the archive, and cleaned its temporary files.                                                                        | Separate assistant/tool-output failures from product-process failures; accept only the child exit code and artifact evidence.              |
| 4        | Can the MTAR be inspected with the initially assumed Java archive tool?                         | Recorded size and hash, then attempted `jar.exe tf`. When `jar.exe` was not installed, opened the MTAR and its nested ZIP payloads with Windows' built-in .NET compression APIs.        | The first listing command failed only at the absent inspection utility. The built-in reader listed the outer archive and both nested module payloads without modifying or extracting them.                                                                 | Do not add a JDK dependency solely for archive inspection; use a platform-native ZIP reader in the future bootstrap verifier.              |
| 5        | Does the archive contain the actual API runtime and reusable component builds?                  | Inspected `flowpilot-api/data.zip` for `dist/server.js` and the package manifests plus compiled `dist/index.js` files for both `@flowpilot/model-adapters` and `@flowpilot/agent-core`. | All five required runtime boundaries were present. The API payload contained 12,649 entries.                                                                                                                                                               | Accept the API module as self-contained for Cloud Foundry staging.                                                                         |
| 6        | Is the SAP UI embedded into the AppRouter rather than missing as a separate runtime module?     | Inspected `flowpilot-approuter/data.zip` for `package.json`, `xs-app.json`, `resources/index.html`, and built JavaScript/CSS assets.                                                    | All required AppRouter and web-content entries were present; the payload contained 5,883 entries.                                                                                                                                                          | Continue treating the web module as build-only content packaged into the AppRouter; do not attempt to start a `flowpilot-web` application. |
| 7        | Could local secrets, Terraform state, plans, or the real BTP profile have leaked into the MTAR? | Searched every nested entry name for `.env` variants, Terraform state/cache/plan artifacts, and `btp.local.json`.                                                                       | The suspicious-entry count was zero. The outer descriptor declared only the API, AppRouter, XSUAA, destination, application-logs, and PostgreSQL deployment graph.                                                                                         | Keep secret and state exclusion as a required post-build gate.                                                                             |
| 8        | Did the final narrow UI check fail because content was missing?                                 | The first PowerShell wrapper placed a pipe after a `finally` block and failed to parse; reran the same read-only membership checks with results formatted inside the protected block.   | `package.json`, `xs-app.json`, and `resources/index.html` each returned `Present=True`. The error was confined to the verification command and did not affect the artifact.                                                                                | Record malformed diagnostics explicitly and require a corrected successful check before drawing a product conclusion.                      |

- Root cause: There was no current failure in the application package. The observed hurdles were long but active compression/dependency phases, assistant polling-output mistakes, and an unavailable optional archive-listing utility.
- Final change: Generated and inspected the local ignored artifact `mta_archives/flowpilot_0.1.3.mtar`; added no application or infrastructure mutation.
- Verification: Strict MBT exited `0` and cleaned temporary files. The artifact is `34,196,737` bytes with SHA-256 `7B0D3EED25A7AB60337D65CDDB5928B3D546C5E68E540FC545806D994E1DBDB1`. Its outer archive contains `META-INF/mtad.yaml`, XSUAA security configuration, and nested API/AppRouter payloads. Required compiled code and SAP UI resources are present, and no environment secret, Terraform state/cache/plan, or real local BTP profile entry was found. No `cf deploy` ran.
- Incorrect or incomplete assumptions corrected: A quiet compression phase is not proof of a hang. Lost or truncated assistant-facing output is not proof of child-process failure. MTAR inspection does not require a JDK because the artifact and its module payloads are ZIP-compatible. A failed diagnostic wrapper is not a failed artifact check until the corrected diagnostic is run.
- Reusable lessons: Keep one long-running build observable instead of starting duplicates. Distinguish command orchestration, build execution, and artifact validation as separate evidence layers. Hash the final artifact, inspect nested runtime boundaries, search for prohibited files, and verify build-only UI placement before any deploy approval.
- Remaining risks or follow-up: The build proves packaging, not a successful deployment or provider call. The MTAR remains intentionally ignored and local. Checkpoint 3A still needs role-assignment, secure-secret, backup/restore interfaces, a guarded bootstrap dry run, and a fresh-clone rehearsal; live deployment remains Checkpoint 4.

### 2026-08-18 — Checkpoint 3A step 8: guarded recovery interfaces

- Objective: Codify post-deployment role assignment, provider-secret entry, portable backup, and safe restore contracts without performing any of those live operations.
- Initial symptom: The repository described these operations at a high level but lacked a versioned local-input schema, provider-validated assignment resources, cross-account data-portability format, and explicit human/destructive gates.
- Constraints and approval boundary: Provider schema reads, official-document review, Terraform validation, and no-save plans were permitted. No role apply, secret entry, service creation, database read/export/import, binding change, Terraform state, or MTA deployment was permitted.
- Evidence collected: SAP/btp `1.25.0` supports subaccount role-collection data reads and user assignments. Its assignment resource requires the subaccount and role collection and accepts a user plus optional custom origin, but its current documentation does not describe assignment import. SAP Credential Store provides protected credential creation inside a namespace. SAP PostgreSQL managed restore is constrained by plan, platform, landscape, and retention, so it cannot replace an external portable export for an expiring trial account.

| Sequence | Hypothesis or question                                                                    | Action or experiment                                                                                                                                                                         | Result                                                                                                                                                                                                                                                | Decision or next step                                                                                                                             |
| -------- | ----------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | What exact role-assignment fields does the pinned provider implement?                     | Read the already-installed provider schema and checked the current official SAP/btp `1.25.0` resource and role-collection data-source documentation.                                         | The resource supports subaccount, role collection, user/group/attribute selectors, and optional identity-provider origin. Assignment import is not documented.                                                                                        | Model user assignments only, validate MTA-owned collections first, prevent destroy, omit duplicates, and keep revocation outside bootstrap.       |
| 2        | Can the existing deployment's application role collections be validated read-only?        | Added an opt-in data-source gate and ran a no-save plan with management disabled.                                                                                                            | `FlowPilotUsers`, `FlowPilotOperators`, and `FlowPilotAdmins` all existed, were read-only, and matched the descriptions from `xs-security.json`; no managed action or state was produced.                                                             | Keep XSUAA/MTA as definition owner and Terraform as post-deployment assignment owner.                                                             |
| 3        | Can the signed-in operator be discovered automatically while keeping plan output private? | Added `btp_whoami`, wrapped its downstream resource user field as sensitive, and ran a no-apply current-admin preview.                                                                       | The resource field was redacted and the plan was 1 add/0 change/0 destroy, but Terraform's data-source read-status line exposed the email as its ID. No file or state captured it, but the output violated the intended normal-plan privacy boundary. | Remove `btp_whoami`; accept the operator only from an ignored operations profile through a sensitive Terraform variable.                          |
| 4        | Did the first privacy-correction patch modify only the intended lines?                    | Applied a multi-file correction, but its expected text did not match formatter-aligned spacing in the example variables file.                                                                | Patch verification failed before changing any file. An exact-text inspection showed the alignment difference.                                                                                                                                         | Retry from inspected content and continue to treat patch verification failures as no-change results.                                              |
| 5        | Does the corrected assignment preview preserve utility without identity discovery?        | Removed `btp_whoami`, marked identity inputs sensitive, supplied a non-real placeholder through a process environment variable, and repeated the no-save plan.                               | The same existing collections were validated; the plan showed 1 add/0 change/0 destroy and `user_name=(sensitive value)`, with no identity data-source log. No apply, saved plan, or state occurred.                                                  | Use the ignored profile for real identities and require plan review before any assignment apply.                                                  |
| 6        | Did opt-in roles accidentally change Terraform's safe default?                            | Ran a final no-save plan with all role and entitlement management flags at their defaults.                                                                                                   | The plan proposed only unsaved outputs, found no missing entitlement, returned an empty validated-role output, and proposed no managed resource changes.                                                                                              | Preserve discovery-only behavior as the normal bootstrap mode.                                                                                    |
| 7        | Can secret metadata be repeatable without storing a provider key?                         | Added a committed example operations profile containing provider, Credential Store instance, namespace, credential name, and type only; defined the permitted cockpit/masked-API entry path. | The versioned contract contains no secret value or placeholder that resembles one. Live provider keys remain prohibited from CLI arguments, configuration files, Terraform, MTA properties, GitHub logs, and AI chat.                                 | Keep key entry/rotation as a human gate and verify existence/model behavior without reading or printing the key.                                  |
| 8        | Are SAP-managed database backups sufficient when the entire trial expires?                | Compared managed restore constraints with the requirement to move into a new trial and mapped both application schemas and identity ownership.                                               | Same-landscape retention cannot guarantee recovery into a replacement account. Both `flowpilot_app` and `flowpilot_graph` plus tenant/subject ownership must move together.                                                                           | Define an authenticated-encryption, external-ciphertext logical format with manifest/hash/read-back verification and no plaintext temporary file. |
| 9        | How can restore avoid weakening privacy just to make old chats visible?                   | Defined an empty-target-only flow with version checks, explicit old-to-new identity mapping, counts, LangGraph reads, forced-RLS tests, rollback retention, and a separate binding switch.   | The default on uncertain identity continuity is that restored rows remain inaccessible; administrator reassignment and RLS bypass are explicitly prohibited.                                                                                          | Keep identity mapping, target creation, import, binding switch, and source deletion behind separate human gates.                                  |

- Root cause: Recovery operations were previously prose expectations rather than provider-checked, versioned interfaces. Automatic identity discovery also had a non-obvious privacy leak in routine Terraform progress output.
- Final change: Added guarded role-collection validation and assignment resources, sensitive ignored-profile identity inputs, `prevent_destroy`, a non-secret operations-profile example, and a dedicated secret/backup/restore contract.
- Verification: Terraform formatting and provider-aware validation pass. Live role validation found all three XSUAA-owned collections. The corrected assignment preview was 1 add/0 change/0 destroy with the user redacted; the final default plan proposed zero managed changes. No saved plan or state exists. JSON/Markdown formatting and whitespace checks pass after the documentation update.
- Incorrect or incomplete assumptions corrected: Marking a downstream Terraform resource attribute sensitive does not redact an upstream data source's read-status ID. Managed point-in-time restore does not equal portable disaster recovery from an expired account. A database restore is not complete until identity continuity, graph state, RLS, and rollback are verified.
- Reusable lessons: Test secret/PII handling against actual tool output, not only schema sensitivity flags. Keep identity inputs local and sensitive. Separate application-role definition from assignment ownership. Design backup around the failure domain being escaped, authenticate before persistence, and make restore a new-target verification flow rather than an overwrite.
- Remaining risks or follow-up: No real role was assigned and no provider secret or private data was handled. The logical backup/restore engine remains a post-deployment implementation task and must be rehearsed before relying on it. The next Checkpoint 3A step is a guarded cross-platform bootstrap dry run that consumes these profiles but cannot deploy or mutate the account.

### 2026-08-18 — Checkpoint 3A step 9: cross-platform bootstrap dry run

- Objective: Compose the proven recovery stages into one platform-neutral command that validates inputs, explains every intended action, verifies the existing environment read-only, and cannot mutate BTP during Checkpoint 3A.
- Initial symptom: Operators still had to assemble profile checks, tool paths, target validation, Terraform checks, MTA evidence, role/secret/data gates, and deployment boundaries from separate runbook sections.
- Constraints and approval boundary: Implement and test a repository-local dry-run/verify CLI. It may read local profiles, versions, current BTP/CF targets, Terraform provider schema, and the existing MTAR. It must reject apply, deploy, secret, backup, restore, execute, and auto-confirm flags before invoking an external command.
- Evidence collected: Node 24 is already the common runtime on Windows and in Cloud Foundry, so a dependency-free `.mjs` command avoids platform-specific PowerShell/Bash orchestration. The current workstation requires safe fallback discovery for user-local BTP/Terraform, Program Files CF/npm, off-PATH GNU Make, and repository-local MBT.

| Sequence | Hypothesis or question                                                              | Action or experiment                                                                                                                                                                                 | Result                                                                                                                                                                                                                  | Decision or next step                                                                                                                              |
| -------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | Can one command remain cross-platform and safe before dependencies or cloud access? | Added `npm run btp:bootstrap` around a Node-standard-library CLI with schema validation, secret-field scanning, target summaries, nine ordered stages, and hard-coded Checkpoint 3A refusals.        | The default dry run read the ignored BTP profile plus committed operations template, printed every effect/gate, omitted user identity values, invoked no subprocess, and reported that no platform mutation ran.        | Keep dry-run as the default and require an explicit `--mode verify` for read-only external checks.                                                 |
| 2        | Are refusal and redaction behaviors deterministic?                                  | Added Node tests for argument parsing, mutation-flag rejection, both profile schemas, secret-bearing fields/provider-key patterns, required backup schemas, plan gates, and a child-process dry run. | Five bootstrap tests passed. The dry-run output omitted the example operator identity. A direct `--deploy` invocation exited `1` with the Checkpoint 3A refusal before external execution.                              | Run these tests inside the root suite so later changes cannot silently weaken the safety boundary.                                                 |
| 3        | Can Windows tool shims be probed with shell execution disabled?                     | First `verify` used `spawnSync(..., shell:false)` against resolved commands, including `.cmd` npm and MBT shims.                                                                                     | Verification stopped before target checks with `EINVAL` for npm and MBT. No platform action occurred. Windows does not execute command-script shims directly through this shell-free API.                               | Inspect the shims and invoke their underlying npm/MBT JavaScript entry points with the current Node binary; do not enable a general command shell. |
| 4        | Does the corrected verifier find the workstation-specific tool locations safely?    | Added explicit candidate resolution for user-local BTP/Terraform, Program Files CF/npm, GnuWin32 Make, and local MBT; repeated verify outside the restricted user-path sandbox.                      | Node `24.12.0`, npm `11.11.1`, Git `2.52.0`, CF `8.18.4`, BTP CLI `2.106.1`, Terraform `1.15.8`, Make `3.81`, and MBT wrapper `1.2.49`/native `1.2.47` were found. BTP and CF matched the ignored profile.              | Preserve shell-free argument arrays and report versions without paths, raw target output, tokens, or identities.                                   |
| 5        | Is command presence enough to call the environment ready?                           | Added compatibility patterns for Node/CF/BTP/Terraform/Make/MBT, checked the MultiApps plugin, ran Terraform format/provider validation, and inspected the local MTAR size.                          | Compatibility and MultiApps passed; the provider configuration validated; `flowpilot_0.1.3.mtar` was present at `34,196,737` bytes. No Terraform plan/state or cloud mutation was created.                              | Treat versions, required plugin, target identity, provider schema, and artifact evidence as separate preflight gates.                              |
| 6        | Will a sequential verifier appear hung during a slower bounded probe?               | The corrected verify crossed the assistant's 30-second observation window during its first run; status was reported, then explicit progress messages were added between probes.                      | The final compatibility-aware verify printed toolchain, BTP, CF, MultiApps, Terraform, and MTAR stages and completed in about 26 seconds.                                                                               | Keep per-command timeouts plus operator-visible stage markers; never rely on a single silent whole-command timeout.                                |
| 7        | Can the full repository suite run immediately after strict production packaging?    | Started the root suite, recognized that the MTA build had pruned API development tools, and restored the API exactly from its lockfile with `npm ci --include=dev`.                                  | The install was silent for roughly a minute but showed active CPU and eventually installed 211 audited packages with zero vulnerabilities. One owned process was monitored; no duplicate install was started.           | Record production-prune workspace mutation and restore locked development dependencies before development verification.                            |
| 8        | Does the complete repository remain green after bootstrap integration?              | Ran the root test command, now including bootstrap tests, with bounded polling across every package/application suite.                                                                               | Five bootstrap tests and 29 existing deterministic tests passed. The optional local PostgreSQL test remained skipped as designed; its real BTP isolation gate was already approved.                                     | Accept the command implementation and proceed to a clean-clone rehearsal without deploying or replacing live resources.                            |
| 9        | Did the final repository scan find a real provider credential?                      | Searched tracked workspace content for the known user identity and Groq-shaped key values. The first scan matched the deliberately fake rejection-test fixture.                                      | No real identity or credential was present, but the static token-shaped fixture would still create avoidable secret-scanner noise. Reconstructed it from separate test strings; the test passed and scan returned zero. | Keep negative secret tests without committing contiguous strings that resemble real credentials.                                                   |

- Root cause: Recovery knowledge existed, but no executable composition enforced ordering and human gates. The implementation-specific hurdle was Windows `.cmd` behavior under a deliberately shell-free child-process policy.
- Final change: Added `npm run btp:bootstrap`, a dependency-free profile validator/planner, secret-material refusal, hard-disabled mutation flags, bounded `verify` mode, cross-platform tool discovery, compatibility/MultiApps/target/Terraform/MTAR checks, visible progress, and five root-integrated tests.
- Verification: Default dry run completed with no external process. `--deploy` failed closed. Read-only verify matched all required versions, BTP/CF targets, MultiApps, Terraform provider configuration, and MTAR size. All four TypeScript boundaries passed type-checking. The full deterministic repository suite passed 34 tests total, with only the intentionally optional local PostgreSQL test skipped. No apply, deploy, role change, secret operation, database operation, state, or saved plan occurred.
- Incorrect or incomplete assumptions corrected: A `.cmd` file found on Windows cannot be executed by Node's shell-free spawn API. Finding an executable does not prove version/plugin compatibility. A command-level timeout is less informative than progress between bounded probes. A strict MTA build can leave source development dependencies pruned even after packaging succeeds.
- Reusable lessons: Prefer a dependency-free cross-platform orchestrator that passes argument arrays directly. Make dangerous verbs unrepresentable in early phases. Test profiles for secret-shaped fields and output for PII. Separate dry-run planning from read-only verification. Resolve tools explicitly without silently changing PATH. Integrate bootstrap safety tests into the ordinary repository gate.
- Remaining risks or follow-up: The command intentionally cannot initialize a missing provider cache, apply prerequisites, build an MTAR, deploy, assign roles, handle keys, or move data yet; it only composes and verifies the proven boundaries. The next step must rehearse the pure dry run from a clean clone where node modules, local profiles, provider cache, and MTAR are absent, while proving no live resource replacement.

### 2026-08-20 — Checkpoint 3A step 10: clean-clone rehearsal and packaging hygiene

- Objective: Rehearse recovery from GitHub in an isolated clean clone, prove locked dependency/provider/MTA restoration without live mutation, and correct any difference between a long-lived workspace and a clean build.
- Continuation (2026-09-01): Resumed after the prior task interruption in a detached worktree; the final quality gates and GitHub-clone proof remain the only pending work in this section.
- Initial symptom: The bootstrap and main-workspace build passed, but a replacement trial depends on what a new clone can recover without untracked caches, generated resources, local profiles, Terraform state, or an existing MTAR.
- Constraints and approval boundary: Use a uniquely named temporary clone, example profiles for the pure dry run, backend-disabled Terraform initialization/validation only, and a strict local MTA build. Do not run Terraform plan/apply, BTP account mutation, CF service/application commands, role assignment, secret handling, database operations, or `cf deploy`. Remove only the validated owned temp clone after evidence is committed.
- Evidence collected: GitHub `main` at `c461de3` cloned with a clean index and no `node_modules`, `.terraform`, local profiles, or `mta_archives`. The installed provider cache in the main workspace contained SAP/btp `1.25.0` for Windows with binary SHA-256 `8D98E7988A32EE8FB9607382E70821C4CCC8827A784EE2A0E9003925AEBC545E`.

| Sequence | Hypothesis or question                                                                     | Action or experiment                                                                                                                                                                                                                      | Result                                                                                                                                                                                                                                                                                                  | Decision or next step                                                                                                                                                             |
| -------- | ------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | Does the bootstrap truly work before npm or Terraform setup?                               | Cloned GitHub `main` into a new GUID-named temp directory, verified a clean worktree and absent caches/artifacts, then invoked the committed Node script with its automatic example fallback.                                             | The nine-stage dry run completed with example/template labels, refused live deployment, printed no identity, and invoked no external process.                                                                                                                                                           | Keep the orchestrator dependency-free; root dependency installation is a later local stage, not a prerequisite for understanding the plan.                                        |
| 2        | Can root tooling be restored exactly from Git?                                             | Ran root `npm ci` in the clean clone.                                                                                                                                                                                                     | Forty-two packages installed from the root lockfile, audit reported zero vulnerabilities, and repository MBT/Prettier became available.                                                                                                                                                                 | Preserve root tooling as a locked, reversible local bootstrap step.                                                                                                               |
| 3        | Can the clean clone download and validate the pinned SAP provider with no backend?         | Ran `terraform init -backend=false -input=false`. The orchestration cell failed to yield as requested and remained pending across a task interruption; on resumption it was terminated and its buffered error was inspected.              | Terraform had made two IPv6 registry requests and both were forcibly reset. No Terraform process, provider file, state, plan, or Git change remained. The lack of prompt status was a collaboration/tool-control failure, not an acceptable silent wait.                                                | Report and terminate over-bound operations immediately on task resumption; inspect partial state before retrying.                                                                 |
| 4        | Was the registry failure transient enough for one safe retry?                              | Repeated the same backend-disabled init once with a short visible bound.                                                                                                                                                                  | It failed quickly after two identical `registry.terraform.io` IPv6 resets.                                                                                                                                                                                                                              | Stop network retries; do not loop, change provider source, weaken verification, or imply that the HCL failed.                                                                     |
| 5        | Can the exact already-verified provider support an offline rehearsal without state?        | Inspected the main cache, copied only its ignored `providers` tree into the temp clone, verified the copied executable size/hash, and ran `terraform validate` without init/plan.                                                         | The copied binary was 22,818,304 bytes with the exact SHA-256 above. Provider-aware validation passed. No backend, state, saved plan, target refresh, or cloud call occurred.                                                                                                                           | Permit an exact-lock/hash local-cache fallback for rehearsals; document that a genuinely new workstation still requires one successful provider download or approved mirror.      |
| 6        | Does a strict MTA build succeed from the isolated clone?                                   | Installed only root tooling, scoped GNU Make to the build process, and let MTA restore every module lockfile and build API/web/AppRouter.                                                                                                 | Strict MBT exited `0`, generated a 34,127,372-byte MTAR with SHA-256 `82C5D0783E5DC64B16FA2629408BD42ECDA4D231DD961AAD98C076698A65717E`, and cleaned temp files. API had 12,649 entries; AppRouter had 5,881; prohibited-entry count was zero.                                                          | Accept the clean-clone dependency/build path, then compare it with the long-lived workspace before calling packaging reproducible.                                                |
| 7        | Why was the clean MTAR smaller than the prior main-workspace artifact?                     | Compared nested entry-name sets instead of relying on outer ZIP size/hash.                                                                                                                                                                | The long-lived AppRouter payload alone contained two stale Vite assets, `index-CYrcceR3.js` and `index-x2kz7gmn.css`, left from an earlier build in the ignored `resources` directory. The clean clone correctly omitted them.                                                                          | Add a guarded MTA pre-build cleanup of generated AppRouter resources and preserve `.gitkeep`.                                                                                     |
| 8        | Can the cleanup be made destructive only inside the exact owned directory?                 | Added a Node cleanup that resolves the real repository/target, requires the exact non-symlink `apps/approuter/resources` directory, preserves `.gitkeep`, and removes only its other children. Added temp-fixture boundary/refusal tests. | Two cleanup tests passed: generated files/directories were removed, `.gitkeep` and an outside sentinel survived, and a missing exact target was refused. The MTA `before-all` step reported removing the two expected generated entries.                                                                | Keep target validation in code and MTA ownership rather than relying on a broad shell deletion or operator memory.                                                                |
| 9        | Did the corrected main build match the clean clone's deployable file set?                  | Rebuilt strictly with the cleanup and compared every nested file path.                                                                                                                                                                    | Build exited `0`; API contained 11,921 files and AppRouter 5,266 files in both artifacts, with no missing/extra entries. The outer artifact was 34,162,843 bytes with SHA-256 `629BCA4F192319D36581110C60CD7EE1BDFCAA0EC9E923CCF6DE1ECB4155FE4F`.                                                       | Treat entry-set equality as proof that stale assets are fixed; continue to inspect remaining content differences.                                                                 |
| 10       | Why were some same-path text files still byte-different across the two Windows workspaces? | Hashed every nested file by uncompressed content.                                                                                                                                                                                         | Twenty-four API and six AppRouter text/source metadata files differed by CRLF versus LF; compiled `dist/server.js` was identical. The older clone predated an explicit repository checkout policy. Outer ZIP hashes also include build metadata and are not a portable content-reproducibility measure. | Add `.gitattributes` with `eol=lf` for text plus binary exclusions, and validate the final policy with a new GitHub clone rather than rewriting an in-use worktree destructively. |
| 11       | Can the ordinary formatter validate the new Git attributes policy?                         | Included `.gitattributes` in the focused Prettier command.                                                                                                                                                                                | Prettier reported that it could not infer a parser, while it formatted every supported file. This is a tool-scope limitation, not an invalid attributes file.                                                                                                                                           | Exclude `.gitattributes` from Prettier and verify it with Git's `check-attr`, exact-line checks, and the final clone's checkout bytes.                                            |
| 12       | Did the first exact attributes-file check prove the policy?                                | Counted physical lines and expected nine.                                                                                                                                                                                                 | The check failed because the file contains nine non-empty rules plus one intentional blank separator. No file changed. The corrected validator compared the nine rules directly; Git reported text `eol: lf` and MTAR `binary` with text/diff unset.                                                    | Validate semantic non-empty rules rather than confusing formatting separators with policy entries.                                                                                |
| 13       | Did the interrupted full-test wrapper leave a product or workspace failure?                | Terminated the pending wrapper after a task interruption, then inspected the available output and checked for Terraform/MTA residue.                                                                                                      | The output buffer was truncated and could not establish pass/fail; no Terraform state/plan, MTA temp directory, or module test binaries were left by that run. A permission-limited Windows process query could not expose command lines, so process presence was checked by permitted names and paths. | Do not infer a test result from a truncated wrapper. Reinstall dependencies in this worktree and rerun the suite with a bounded command.                                          |
| 14       | Can dependencies be restored on this resumed worktree?                                     | Ran locked installs. The shared Windows npm cache first failed with `EPERM` while reading one cacache entry; reran all installs with the isolated workspace-local `.npm-cache`.                                                           | Root (42 packages), model adapters (71), agent core (69), API (211), web (140), and AppRouter (204) packages restored. Root/API/web/package installs reported zero vulnerabilities; AppRouter retained three existing moderate audit findings and no `audit fix` was applied.                           | Use a workspace-local cache when the shared cache is locked; never change dependency versions during a recovery rehearsal.                                                        |
| 15       | Does the resumed worktree pass the ordinary quality gates?                                 | Ran `npm test`, `npm run typecheck`, `git diff --check`, and `npm run format:check`; normalized supported files with Prettier while excluding the non-Prettier `.gitattributes` policy file.                                              | Full suite passed 36 tests with one optional local-PostgreSQL test skipped; all type checks passed; whitespace and format checks passed. The first format check had exposed CRLF in the detached worktree, which normalization corrected.                                                               | Commit the packaging correction and documentation, then make one final GitHub clone to prove `.gitattributes` checkout behavior and clean-clone MTA recovery.                     |
| 16       | Does the pushed correction checkout cleanly with repository EOL rules applied?             | Cloned `origin/main` at `cd1580d`, checked Git status, representative bytes, attributes, and the dependency-free bootstrap dry run.                                                                                                       | The clone was clean, had no caches/artifacts, representative text files had LF/no CR bytes, MTAR files were `binary`, and the nine-stage dry run invoked no external command.                                                                                                                           | Continue recovery from this exact GitHub checkout; never rely only on the long-lived development workspace.                                                                       |
| 17       | Can all locked dependencies and shared packages be restored in that final clone?           | Ran root, package, API, web, and AppRouter `npm ci` commands, built both shared packages, and used a clone-local npm cache.                                                                                                               | All lockfiles restored; every package build succeeded. AppRouter retained the same three moderate audit findings reported earlier; no dependency update or audit fix was applied.                                                                                                                       | Keep dependency restoration locked and cache-isolated for trial recovery.                                                                                                         |
| 18       | Does the final GitHub checkout pass application quality gates before packaging?            | Ran the complete root test suite and explicit typecheck in the final clone.                                                                                                                                                               | Thirty-six tests passed with one optional local-PostgreSQL test skipped; all type checks passed.                                                                                                                                                                                                        | Proceed to strict packaging only after the application gates pass.                                                                                                                |
| 19       | Why did the first final-clone strict MTA attempt time out?                                 | Ran `npm run mta:build` with a three-minute bound, inspected exact child processes/output, stopped only the orphaned build PIDs, and removed the validated partial temp directory.                                                        | API packaging completed, but web-module `npm ci` used the shared cache and hit MBT's ten-minute module timeout; the wrapper exited 124 and left no final archive.                                                                                                                                       | Propagate `NPM_CONFIG_CACHE` to MBT subprocesses; document and clean orphaned build processes instead of silently retrying.                                                       |
| 20       | Does the MTA build succeed when every internal npm process uses the clone-local cache?     | Retried once with `NPM_CONFIG_CACHE=<clone>/.npm-cache`, GNU Make scoped to the build, and a ten-minute outer bound.                                                                                                                      | Strict MBT exited `0`, built API/web/AppRouter, removed its temporary files, and generated a 34,130,048-byte MTAR with SHA-256 `4E2CB32C3CC174DDBB3B607D0669B5EBFDF92CFDE3DD3B7A81EEC329B3CC4191`.                                                                                                      | Accept this as the final clean-clone packaging path; retain the first timeout as a reproducibility hurdle.                                                                        |
| 21       | Does the final archive contain the required runtime and no local-state/secret artifacts?   | Inspected outer and nested `data.zip` manifests, required API/AppRouter entries, stale hashed assets, prohibited paths, and the exact provider-cache hash; ran offline `terraform validate`.                                              | API had 11,921 files and both compiled `@flowpilot` packages plus `dist/server.js`; AppRouter had 5,266 files and 263 built assets; required entries were present, stale assets were absent, prohibited count was zero, and Terraform validation passed.                                                | Keep archive inspection and offline validation as mandatory pre-deploy gates.                                                                                                     |
| 22       | Can live target verification run from the final clone today?                               | Ran `btp:bootstrap --mode verify` with the existing ignored local profile and the committed operations template.                                                                                                                          | The command reached the real-target check and failed closed because the current browser SSO session had expired; it instructed `btp login --sso`. No mutation ran.                                                                                                                                      | Leave SSO refresh as an explicit human gate; do not automate credentials or claim live verification while the session is expired.                                                 |
| 23       | Is the final audit documentation itself format-clean after the new rows?                   | Ran the full formatter in the updated clone, which found only `DevFlow.md`; formatted that file, staged a documentation-only correction, and rechecked it.                                                                                | The documentation-only correction is format-clean and does not alter application behavior. A fresh pull of the follow-up commit must rerun the format/whitespace check before cleanup.                                                                                                                  | Push the follow-up documentation commit, refresh the final clone, verify its clean status and format, then close the rehearsal gate.                                              |
| 24       | Did the final GitHub state remain clean after documentation and cleanup?                   | Pushed `9c997d1`, fast-forwarded the final clone, reran `format:check`, `git diff --check`, and Git status, then removed both validated temporary clones and rechecked processes/residue.                                                 | The final clone was clean at `9c997d1`; formatting and whitespace passed; no build process, temp clone, Terraform state, or plan remained.                                                                                                                                                              | Close the clean-clone rehearsal. Keep live SSO refresh and Checkpoint 3A human approval as explicit next gates.                                                                   |
| 25       | Why did the first staging attempt fail after verification?                                 | Ran `git add` from the detached worktree. The sandbox denied creation of the linked worktree's `.git/worktrees/.../index.lock`; retried only the Git metadata operation with approved access.                                             | No source file changed and the staged diff was then exactly the seven intended packaging/documentation files.                                                                                                                                                                                           | Treat Git metadata permissions as an execution-environment hurdle; request narrowly scoped access rather than changing repository paths.                                          |
| 26       | Why does this older worktree still show line-ending-only status markers?                   | Compared `git status`, `git diff`, and `git ls-files --eol` after the final push.                                                                                                                                                         | `core.autocrlf=true` plus the pre-existing checkout leaves CRLF working bytes and status markers, while the normalized content diff is empty; a new GitHub clone at the final commit is clean with LF bytes.                                                                                            | Use a fresh clone or re-checkout after adding `.gitattributes`; do not interpret these markers as uncommitted semantic changes.                                                   |

- Root cause: A long-lived ignored AppRouter resource directory accumulated obsolete hashed UI files, and the repository did not declare consistent cross-platform text checkout bytes. The external provider registry also experienced repeatable IPv6 resets unrelated to the configuration.
- Final change: Added guarded AppRouter resource cleanup to MTA `before-all`, two safety tests integrated into the root suite, and a repository-wide LF text policy with binary exclusions. The resumed worktree and final GitHub clone pass the full suite and type/format gates after isolated npm-cache recovery; final commits `cd1580d`, `8180e35`, `9c997d1`, `2fa4fcb`, and `0e53e4c` are pushed to `origin/main`.
- Verification: Dependency-free dry runs, locked installs, offline provider-aware validation, strict clean-clone build, strict corrected main build, final-clone strict build, secret/state exclusion, exact nested entry-set comparison, cleanup boundary tests, 36 deterministic tests, type checks, formatting, Git cleanliness, and temporary-clone cleanup pass. No live BTP/CF mutation or deployment ran.
- Incorrect or incomplete assumptions corrected: A successful build from a long-lived workspace can package ignored stale assets. Equal module counts do not prove equal file sets. Outer archive hashes are build records, not automatically reproducible-content identifiers. Backend-disabled init still requires registry availability unless an approved cache/mirror is present.
- Reusable lessons: Rehearse from Git, not only the developer workspace. Clean generated handoff directories at the declarative build boundary with exact-path guards. Compare nested path/content manifests instead of only MTAR size. Declare repository EOL policy. Stop repeated registry failures and use only lock/hash-verified offline inputs.
- Remaining risks or follow-up: Live BTP verification still needs the human SSO refresh (`btp login --sso`); Checkpoint 3A human review remains required before any Checkpoint 4 deployment, secret entry, backup/restore, or other cloud mutation. The final MTAR outer hash is a build record, while nested entry/content checks are the reproducibility evidence.

### 2026-08-16 — BTP authentication callback failure

- Objective: Complete SAP ID authentication through XSUAA and AppRouter, then prove that the authenticated browser can call the protected `/api/me` endpoint.
- Initial symptom: After authenticating, the browser displayed `Authorization Request Error` and reported that the OpenID provider could not process the request because of configuration issues.
- Constraints and approval boundary: Diagnose and fix the deployed authentication foundation in the user's BTP trial space; preserve OAuth security protections; do not begin the chat implementation.
- Evidence collected: Both Cloud Foundry applications were healthy; direct access to the API's protected endpoint returned `401`; the browser reached XSUAA and SAP ID but initially did not return through the AppRouter callback; the requested callback was `https://7d472741trial-dev-flowpilot-approuter.cfapps.us10-001.hana.ondemand.com/login/callback`.

| Sequence | Hypothesis or question                                                                                 | Action or experiment                                                                                                                                                                                                                                                   | Result                                                                                                                                                                                                                                                      | Decision or next step                                                                  |
| -------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| 1        | The deployed applications or route might be unavailable.                                               | Checked Cloud Foundry application health, the API health endpoint, and direct protected API behavior.                                                                                                                                                                  | Both applications were running, health returned `200`, and direct `/api/me` correctly returned `401`.                                                                                                                                                       | Focus on the OAuth/OpenID redirect flow rather than application availability.          |
| 2        | AppRouter 23's default PKCE parameters might be incompatible with the trial subaccount's SAP ID trust. | Set `PKCE_ENABLED` to `false`, built and deployed MTA 0.1.1, and repeated login.                                                                                                                                                                                       | OAuth state remained present, but the same provider error occurred.                                                                                                                                                                                         | The PKCE hypothesis was not supported; do not accept this as the fix.                  |
| 3        | PKCE might still be present elsewhere in the provider chain.                                           | Inspected the live authorization URLs in the browser after SAP ID login.                                                                                                                                                                                               | XSUAA independently added a PKCE challenge on its request to SAP ID, and the failure occurred after provider authentication.                                                                                                                                | Restore AppRouter PKCE and investigate provider/client configuration.                  |
| 4        | The default SAP ID trust or trial control plane might be misconfigured.                                | Compared the behavior with SAP documentation and the trial cockpit; observed a cockpit console message referencing `cf-eu10` while the Cloud Foundry target was `us10-001`.                                                                                            | The region inconsistency was suspicious but did not prove the application login root cause.                                                                                                                                                                 | Record it as inconclusive and continue with application-specific OAuth configuration.  |
| 5        | XSUAA might be rejecting an unregistered non-local callback URI.                                       | Checked SAP's redirect-URI guidance and found that cloud callbacks must be explicitly registered.                                                                                                                                                                      | `xs-security.json` and the deployed MTA had no explicit AppRouter login callback registration.                                                                                                                                                              | Add an exact callback without hard-coding organization, space, route, or region.       |
| 6        | An MTA provided property could inject `${default-url}/login/callback` into XSUAA.                      | Added an AppRouter `provides` property and initially placed `oauth2-configuration` under the resource dependency's `requires.parameters`; tried the full `~{flowpilot-approuter-binding/redirect-uri}` reference.                                                      | Deployment could not resolve the expression, so the operation was aborted.                                                                                                                                                                                  | Recheck descriptor structure instead of weakening the callback pattern.                |
| 7        | A shorter property reference might resolve in that nesting.                                            | Changed the reference to `~{redirect-uri}`, built and deployed MTA 0.1.2, and retried authentication.                                                                                                                                                                  | The authentication error persisted. A later parameter-check script printed a blank URI, but the raw Cloud Foundry response showed that XSUAA does not support fetching instance parameters; the blank output was therefore an invalid diagnostic inference. | Correct the diagnostic record and compare the descriptor with an official SAP example. |
| 8        | The OAuth configuration was nested at the wrong MTA level.                                             | Compared the descriptor with SAP's official Cloud CAP MTA sample. Moved the provider dependency to the XSUAA resource's `requires` and moved `oauth2-configuration` to the resource's `parameters.config`; restored PKCE; used the fully qualified property reference. | Strict MTA build succeeded and MTA 0.1.3 deployed with both applications healthy.                                                                                                                                                                           | Perform a complete browser callback test before declaring success.                     |
| 9        | The corrected callback registration should complete the full flow.                                     | Started a fresh authorization request in the authenticated browser and observed the final UI state.                                                                                                                                                                    | SAP ID returned through XSUAA and AppRouter to FlowPilot. The UI displayed the authenticated user; that UI state is reached only after `/api/me` returns successfully.                                                                                      | Accept the callback registration as the supported root cause and final fix.            |

- Root cause: The AppRouter's non-local `/login/callback` URI was not registered in XSUAA. The first dynamic-registration attempts also placed `oauth2-configuration` at the wrong MTA level.
- Final change: MTA 0.1.3 keeps OAuth state and PKCE enabled, exposes the exact AppRouter callback as a provided property, declares the provider dependency under the XSUAA resource's `requires`, and injects the URI under `parameters.config.oauth2-configuration.redirect-uris`.
- Verification: Strict MTA build passed; both Cloud Foundry applications reported `1/1` healthy; the real SAP ID redirect and callback completed; the frontend reached its authenticated state after the protected `/api/me` request succeeded; formatting, type-checking, three automated tests, and production builds passed.
- Incorrect or incomplete assumptions corrected: Disabling AppRouter PKCE was not the fix. A blank value printed after parsing an unsupported service-parameter endpoint was not evidence of the stored XSUAA configuration.
- Reusable lessons: Test the complete identity-provider callback, not only the initial redirect. Check an API response for errors before reading expected fields. For MTA resources, keep dependencies under `requires` and service configuration under `parameters.config`. Compare unusual platform configuration with a current primary-source sample before deploying another variation.
- Remaining risks or follow-up: Preserve the real-user authentication smoke test when adding chat persistence and roles. User-to-user chat-history isolation still needs dedicated persistence and authorization tests in a later, explicitly approved phase.

### 2026-08-16 — Private-chat design constraints

- Objective: Define the first useful chat slice without starting implementation, incorporating SAP-standard theming, Groq as the default provider, secure third-party credentials, PostgreSQL persistence, and the trial account's single-user limitation.
- Initial symptom: The deployed application proved authentication but remained a custom-gradient status page with no chat, persistence, model call, or practical two-user cloud test path.
- Constraints and approval boundary: Produce Checkpoint 1 design and teaching material only. Do not provision services or implement feature code before human review.
- Evidence collected: The repository uses hard-coded colors and gradients; the targeted marketplace exposes `postgresql-db` with a `trial` plan and `credstore` with a `trial` plan; the user reported that a second trial user cannot be created; current Groq and LangChain documentation lists the Groq adapter and `llama-3.3-70b-versatile` as a production model.

| Sequence | Hypothesis or question                                                          | Action or experiment                                                                                                 | Result                                                                                                                                                                             | Decision or next step                                                                                                                            |
| -------- | ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1        | Which SAP-native UI approach fits the existing React application?               | Reviewed the current custom CSS and the official UI5 Web Components theme configuration.                             | UI5 Web Components supports React usage and includes `sap_horizon` as its standard default theme.                                                                                  | Adopt UI5 Web Components for React, remove gradients, and limit custom CSS to SAP theme variables.                                               |
| 2        | Can Groq remain the default without coupling LangGraph to Groq?                 | Checked the current LangChain Groq adapter and provider-neutral chat-model interfaces.                               | `@langchain/groq` implements the common chat-model contract used by LangGraph.                                                                                                     | Inject a provider-neutral model into the graph; support Groq, OpenAI, and Anthropic adapters behind one factory.                                 |
| 3        | Which Groq model is suitable as a configurable default?                         | Checked Groq's current production-model catalog and deprecation guidance.                                            | `llama-3.3-70b-versatile` is currently listed as a production model, but provider model IDs can be deprecated.                                                                     | Use it as the initial default while keeping the model ID in server configuration and checking deprecations before releases.                      |
| 4        | Should the API key be stored as an ordinary Cloud Foundry environment variable? | Compared Cloud Foundry credential guidance with SAP Credential Store capabilities and checked the trial marketplace. | Cloud Foundry advises service bindings for sensitive values; Credential Store is designed for BTP application passwords and keys and is available in the trial space.              | Store deployed provider keys in Credential Store; keep only non-secret model selection in MTA properties.                                        |
| 5        | Can privacy be proven without a second BTP trial identity?                      | Separated automated identity/isolation evidence from deployed real-user evidence.                                    | A real two-user trial smoke test is unavailable, but unit and PostgreSQL integration tests can execute the same authorization paths with two distinct validated-identity fixtures. | Require automated two-identity negative tests, a deployed single-user unknown-ID denial test, and explicitly defer the real two-user cloud test. |
| 6        | Is the intended persistence service actually available?                         | Queried the targeted Cloud Foundry marketplace.                                                                      | `postgresql-db` with a `trial` plan is available.                                                                                                                                  | Use BTP PostgreSQL for the conversation catalog and LangGraph checkpoint state.                                                                  |
| 7        | Can the normal repository formatter validate the local environment template?    | Included `.env.example` in a focused Prettier check.                                                                 | Prettier could not infer a parser for the environment file, while Markdown formatting and local-link checks passed.                                                                | Exclude environment templates from Prettier and validate them separately as uppercase key/value records.                                         |

- Root cause: Not applicable; this was a design checkpoint. The main constraints were an intentionally minimal authentication-only UI, provider-secret handling requirements, and the lack of a second trial identity.
- Final change: Added ADRs for SAP Horizon UI, Groq-first provider-neutral models, and PostgreSQL isolation; added the private-chat delivery plan and secure BTP LLM configuration guide; added safe local model placeholders; updated product, architecture, and repository milestone documentation.
- Verification: Cross-checked choices with the live BTP marketplace and current primary documentation from SAP, Cloud Foundry, LangChain, UI5, and Groq. Prettier passed for all formatted documents, all local Markdown links resolved, the environment template passed its separate key/value validation, and `git diff --check` passed.
- Incorrect or incomplete assumptions corrected: A real two-user BTP smoke test cannot be treated as an MVP trial gate. Ordinary environment variables are not the preferred location for provider API keys. Model portability means switching among installed and tested adapters without graph changes, not loading an arbitrary unreviewed provider from the browser.
- Reusable lessons: Separate cloud-environment evidence from automated security evidence; disclose missing production-grade tests. Store provider secrets behind a platform secret service, keep model selection server-side, and use provider projects and limits per environment. Treat model IDs as replaceable configuration because provider catalogs change.
- Remaining risks or follow-up: Checkpoint 2 requires explicit approval. The actual Credential Store retrieval contract, PostgreSQL row-level policies, model adapter behavior, and two-identity tests must be implemented and verified. A real deployed two-user test remains required before production use.

### 2026-08-16 — Checkpoint 2 private-chat backend implementation

- Objective: Implement the approved backend-only checkpoint: reusable model adapters, a minimal LangGraph workflow, PostgreSQL ownership and checkpoints, protected conversation APIs, and automated two-identity denial tests.
- Initial symptom: The API exposed only `/api/me`; no conversation, database, graph, or model runtime existed.
- Constraints and approval boundary: Do not implement the SAP chat UI or deploy the application. Use Groq by default, keep tests provider-free, use actual PostgreSQL for the database gate, and stop after Checkpoint 2 review.
- Evidence collected: Current API and MTA structure were inspected; Docker CLI is installed but its daemon is not running; no local PostgreSQL client or service is installed; BTP marketplace confirmed `postgresql-db/trial`; selected LangChain packages support Node 24 and share compatible `@langchain/core` versions. SAP documents that BTP PostgreSQL enforces SSL, and node-postgres documents that connection-string SSL parameters can override separately supplied settings.

| Sequence | Hypothesis or question                                                                                              | Action or experiment                                                                                                                                                                                                                                                                                                                                                            | Result                                                                                                                                                                                                                                                                | Decision or next step                                                                                                                                         |
| -------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1        | How can reusable TypeScript packages remain independently testable and still be packaged inside the API MTA module? | Added standalone `agent-core` and `model-adapters` packages, packed them into the API through local file dependencies with `install-links=true`, and bundled API-owned source with esbuild while leaving installed packages external.                                                                                                                                           | Package boundaries compile independently and the API can ship copied package contents rather than broken links outside its MTA module.                                                                                                                                | Keep package build/install ordering explicit in root and MTA commands.                                                                                        |
| 2        | Can privacy tests run against local PostgreSQL?                                                                     | Checked Docker, `psql`, and Windows services.                                                                                                                                                                                                                                                                                                                                   | Docker daemon, `psql`, and a local PostgreSQL service are unavailable.                                                                                                                                                                                                | Keep unit tests local but require the approved BTP PostgreSQL trial instance for the real RLS/checkpoint test.                                                |
| 3        | Why did the new standalone packages initially fail type-checking?                                                   | Ran package type-checks immediately after adding their sources.                                                                                                                                                                                                                                                                                                                 | Each package lacked its own Node type declarations; relying on the API's dev dependency does not work across a standalone package boundary.                                                                                                                           | Add `@types/node` and `types: ["node"]` to each owning package.                                                                                               |
| 4        | Can the approved BTP PostgreSQL service be provisioned for integration testing?                                     | Ran `cf create-service postgresql-db trial flowpilot-postgres`.                                                                                                                                                                                                                                                                                                                 | Cloud Foundry rejected the request because the CLI token expired; no service was created.                                                                                                                                                                             | Ask the user to repeat `cf login --sso`, continue safe local work, and leave the real database gate open.                                                     |
| 5        | Do adapter, graph, API, validation, and simulated isolation paths work without provider or database credentials?    | Added fake-model and in-memory repository tests and ran package/API suites.                                                                                                                                                                                                                                                                                                     | Groq/OpenAI/Anthropic construction, LangGraph thread separation, conversation flows, invalid input rejection, and two-identity list/read/write denial passed; the real PostgreSQL test was correctly skipped without `TEST_DATABASE_URL`.                             | Preserve fake dependencies for deterministic tests and run the separate PostgreSQL suite when CF authentication is restored.                                  |
| 6        | Why did the API bundle fail after tests and type-checks passed?                                                     | Anchored build paths to `build.mjs`, retried, then reran the same build outside the filesystem sandbox after repeated parent-directory read denials.                                                                                                                                                                                                                            | The build succeeded outside the sandbox. Esbuild's package resolution traverses parent directories that the managed sandbox blocks; the code itself was valid.                                                                                                        | Keep the more portable anchored paths and classify the remaining failure as a tool-sandbox constraint, not an application build defect.                       |
| 7        | Does the complete production MTA contain runnable copies of the local packages?                                     | Restored the GNU Make path, ran `mbt build --strict=true`, then inspected the API module's nested archive without extracting secrets or changing the artifact.                                                                                                                                                                                                                  | The strict build passed for API, web, and approuter. The API archive contains both `@flowpilot` packages, their manifests, and compiled `dist` files; all build-time npm audits reported zero vulnerabilities.                                                        | Keep the strict MTAR build and nested-package inspection as release evidence.                                                                                 |
| 8        | Can cloud database work resume after the expired-token failure?                                                     | Rechecked the target outside the restricted filesystem sandbox, confirmed the expected user/org/space, verified the service did not exist, and retried the approved `postgresql-db/trial` creation.                                                                                                                                                                             | The refreshed session was valid and the broker accepted the request. The `flowpilot-postgres` service is currently creating; the application remains undeployed.                                                                                                      | Wait for `create succeeded`, create a short-lived test service key, run the real isolation gate without printing credentials, then delete only that test key. |
| 9        | Will partial runtime startup failures release database resources?                                                   | Reviewed the production startup path after the successful build and added failure cleanup for both the application pool and LangGraph checkpointer. The first type-check exposed an optional-value capture inside the returned close callback.                                                                                                                                  | Startup cleanup now uses `Promise.allSettled`; retaining a narrowed local checkpointer reference corrected the TypeScript closure error, and the focused API type-check passed.                                                                                       | Include failure-path resource ownership in every startup review, and rerun the full build after this correction.                                              |
| 10       | Do malformed and oversized JSON requests remain safe client errors?                                                 | Reviewed the Express error path, found that body-parser failures would otherwise reach the generic `500`, added safe `400`/`413` mappings, and added malformed/oversized payload tests.                                                                                                                                                                                         | Both payload cases now return stable error codes without echoing the body or parser detail.                                                                                                                                                                           | Test framework and middleware errors in addition to schema-valid but invalid business input.                                                                  |
| 11       | Can the real database test use a temporary local service key?                                                       | Waited for `create succeeded`, created `flowpilot-checkpoint2-test`, retrieved only field names, and attempted the integration test without printing values. The first parser assumed direct credentials; the broker key actually wrapped them in `credentials`.                                                                                                                | The first attempt failed with `Invalid URL`. After nested parsing was added, both the SQL attempt and a separate TCP check timed out because the database endpoint is not reachable from the local machine. No schema was created, and the temporary key was deleted. | Support both direct and nested binding shapes; run the gate inside the Cloud Foundry network rather than weakening network controls.                          |
| 12       | Can an existing app provide a non-mutating tunnel?                                                                  | Checked SSH on the deployed API and AppRouter; both were disabled. The first `cf space-ssh-allowed` call omitted its required space argument, then the corrected `cf space-ssh-allowed dev` confirmed the space permits SSH.                                                                                                                                                    | Using an existing app would require enabling SSH and restarting it, crossing the no-FlowPilot-deployment boundary.                                                                                                                                                    | Leave both project apps untouched and create an isolated, no-route temporary runner bound only to PostgreSQL.                                                 |
| 13       | Will the first temporary runner execute the integration suite?                                                      | Pushed `flowpilot-checkpoint2-runner` with no route. Its keepalive opened no port, so the default health check stayed in `starting`; switched only the runner to process health. The first task then failed because CF's production environment made nested `npm ci` omit TypeScript/Vitest.                                                                                    | The runner became healthy after the process check. The next task used `--include=dev`, compiled both reusable packages, installed the API, and reached the database test.                                                                                             | Make task health semantics and development-tool installation explicit for disposable verification runners.                                                    |
| 14       | Why did the database reject the CF task as unencrypted?                                                             | Confirmed from SAP documentation that SSL is enforced. Added CA-backed TLS from `sslrootcert`/`sslcert`, shared one pool with LangGraph, and parsed the URL before applying SSL. Multiple tasks still attempted plaintext. A first safe `tsx` diagnostic failed because top-level await was emitted as CommonJS; the corrected diagnostic showed TLS was absent only inside CF. | The Node buildpack injects a plain `DATABASE_URL`; the resolver preferred it over the certificate-bearing `VCAP_SERVICES` binding. Reversing precedence activated CA validation.                                                                                      | Prefer a matching platform binding over buildpack convenience variables; retain `DATABASE_URL` as the local/fallback path and test precedence explicitly.     |
| 15       | Did the first encrypted end-to-end run fully preserve chat history?                                                 | Reran the cloud gate after the precedence correction. Migrations, RLS checks, cross-user denials, and checkpoint writes all executed, but the last reload assertion found a different assistant message ID.                                                                                                                                                                     | LangGraph's PostgreSQL rehydration did not preserve the fake assistant response ID consistently; generating a new random fallback made API IDs unstable across reads.                                                                                                 | Derive deterministic API message IDs from the private thread, position, role, and content; add stability/uniqueness regression checks.                        |
| 16       | Does the corrected backend pass the real gate without leaving test resources?                                       | Restaged only the disposable runner and ran `checkpoint2-postgres-6`. After success, collected redacted logs, deleted the runner, validated and removed its local temporary directory, and verified cloud cleanup.                                                                                                                                                              | The real test passed: schema setup, forced RLS, no-identity denial, two-subject isolation, LangGraph persistence, and stable reload. No service keys or runner remain; `flowpilot-postgres` is healthy, unbound, and retained for FlowPilot.                          | Accept the database gate, run final repository/MTA verification, and stop for Checkpoint 2 review.                                                            |
| 17       | Does the database suite still skip cleanly when no local database is configured?                                    | Ran the final root test suite after making pool creation TLS-aware. Vitest evaluated module-level pool creation before `describe.skipIf` could skip, so the suite failed while reading an absent config.                                                                                                                                                                        | Moving pool creation into the guarded `beforeAll` restored the intended no-database skip path without changing the already-passed cloud path.                                                                                                                         | Keep optional-integration resource creation inside test hooks, then rerun the entire verification sequence.                                                   |
| 18       | Does the final strict MTA command itself exit successfully?                                                         | Ran the strict final build after all tests. MBT generated a readable archive containing the API module, then Windows denied removal of the generated `flowpilot-api/data.zip`, so MBT exited nonzero during cleanup.                                                                                                                                                            | Verified the archive, validated and removed only `.BTPApp_mta_build_tmp` after the transient lock cleared, then reran the complete strict build. The retry built every module, generated the final archive, cleaned its temporary files, and exited `0`.              | Preserve both the cleanup failure and clean retry in the audit; accept only the successful retry as final build evidence.                                     |

- Root cause: The application had no chat runtime or persistence. The implementation-specific failures were independent: missing package-owned Node types; a restricted esbuild traversal; an expired CF token; a nested service-key shape; a CF-internal database endpoint; a temporary runner using the wrong health/dependency defaults; TLS certificates being bypassed by the buildpack's higher-precedence plain `DATABASE_URL`; and unstable randomly generated message IDs after checkpoint rehydration.
- Final change: Implemented reusable Groq-default model adapters, a provider-neutral LangGraph workflow with bounded model context, PostgreSQL migrations and forced row-level ownership, TLS-aware direct/nested binding discovery, one shared pool for repository/checkpoints, concurrency control, protected conversation APIs, safe request/provider error mappings, deterministic message IDs, MTA PostgreSQL configuration, and deterministic plus real database tests.
- Verification: Formatting, all package and application type-checks, 20 deterministic tests, the API production bundle, and the strict full MTAR build pass. The real bound BTP PostgreSQL test also passed schema setup, forced RLS, direct no-identity denial, two-subject list/read/write isolation, LangGraph checkpoint persistence, and stable reload. The archive contains compiled copies of both reusable packages; npm audits reported zero vulnerabilities. The temporary service key, CF runner, and local staging directory were removed; the healthy unbound PostgreSQL service remains.
- Incorrect or incomplete assumptions corrected: An installed Docker CLI does not prove Docker is usable. A package cannot rely on another package's development-only Node types. The esbuild failure was a sandbox traversal restriction, not unresolved application source. A service key and a VCAP binding can expose different nesting. A service key does not imply off-platform network reachability. A valid database URI alone is insufficient when SAP enforces TLS. On CF, buildpack-generated `DATABASE_URL` must not outrank a certificate-bearing service binding. Persisted message content does not guarantee that a framework preserves its optional message ID.
- Reusable lessons: Test every new package at its own boundary. Separate deterministic model/API tests from billable provider tests. Keep a real database gate for security features that an in-memory implementation cannot prove. Treat platform bindings as structured contracts and inspect field names without printing values. Prefer in-platform tasks for internal services. Verify encryption explicitly and define configuration precedence. Validate identifiers across persistence/reload boundaries, not just content. When a build tool reports access errors outside the workspace, rerun the exact command with authorized filesystem access before changing architecture.
- Remaining risks or follow-up: Checkpoint 2 needs human review. The FlowPilot application has not been redeployed with this backend, no Groq request has been made, and Credential Store retrieval and the SAP Horizon UI remain later checkpoints. The real two-user BTP test remains unavailable in the trial account and must be performed before production use; current evidence uses two independent validated-identity fixtures.

## How to maintain this document

- Review it at the beginning and end of each major phase.
- Add lessons that are broadly reusable; keep application-specific details in project documentation.
- Change a rule when evidence shows a better process, and record why.
- Keep phase gates stable enough to compare projects over time.
- Periodically copy the generic sections into a personal template repository.
