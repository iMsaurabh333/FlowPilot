import { Button } from "@ui5/webcomponents-react/Button";
import { useEffect, useRef, useState, type ChangeEvent } from "react";
import type { FlowPilotApi, ReconciliationPreview, ReconciliationResult, ReportSource } from "./api";

const MAX_BYTES = 1_000_000;
const DIRECT_LIMIT = 10;
function base64(file: File) { return new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onerror = () => reject(reader.error); reader.onload = () => resolve(String(reader.result).split(",", 2)[1] ?? ""); reader.readAsDataURL(file); }); }
function title(name: string) { return name.replace(/__.*/u, "").replace(/[-_]/g, " "); }

export function ReconciliationView({ client }: { client: FlowPilotApi }) {
  const upload = useRef<HTMLInputElement>(null);
  const [preview, setPreview] = useState<ReconciliationPreview>(); const [directIds, setDirectIds] = useState(""); const [sources, setSources] = useState<ReportSource[]>([]); const [column, setColumn] = useState(""); const [selected, setSelected] = useState<string[]>([]); const [result, setResult] = useState<ReconciliationResult>(); const [error, setError] = useState<string>(); const [busy, setBusy] = useState(false);
  useEffect(() => { if (!client.listReconciliationSources) return; void client.listReconciliationSources().then(setSources).catch(() => setError("Compatible reconciliation systems could not be loaded.")); }, [client]);
  const choose = async (event: ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; event.target.value = ""; if (!file || !client.previewReconciliationUpload) return; if (file.size > MAX_BYTES) { setError("Upload must be 1 MB or smaller."); return; } setBusy(true); try { const next = await client.previewReconciliationUpload(file.name, await base64(file)); setPreview(next); setColumn(next.headers[0] ?? ""); setDirectIds(""); setResult(undefined); setError(undefined); } catch { setError("Use a readable .xlsx or .csv file with headers and no more than 60 records."); } finally { setBusy(false); } };
  const idsToRun = () => preview && column ? preview.rows.map((row) => row[preview.headers.indexOf(column)]).filter(Boolean) : [...new Set(directIds.split(/[\s,;\n]+/u).map((id) => id.trim()).filter(Boolean))];
  const run = async () => { const ids = idsToRun(); if (!ids.length || !selected.length || !client.runReconciliation) return; if (!preview && ids.length > DIRECT_LIMIT) { setError(`Enter no more than ${DIRECT_LIMIT} IDs directly. Upload Excel or CSV for larger reconciliations.`); return; } setBusy(true); try { setResult(await client.runReconciliation({ ids, sourceToolNames: selected, fields: [] })); setError(undefined); } catch { setError("The reconciliation could not run. Confirm that each selected system is healthy and supports Application Message ID lookup."); } finally { setBusy(false); } };
  const download = async () => { if (!result || !client.downloadReconciliationReport) return; setBusy(true); try { await client.downloadReconciliationReport(result); } catch { setError("The Excel report could not be downloaded."); } finally { setBusy(false); } };
  const directCount = [...new Set(directIds.split(/[\s,;\n]+/u).map((id) => id.trim()).filter(Boolean))].length;
  const systems = result ? [...new Set(result.rows.flatMap((row) => Object.keys(row.systems)))] : [];
  return <section className="reconciliation" aria-labelledby="reconciliation-title"><header><div><p className="section-label">Manual reconciliation</p><h2 id="reconciliation-title">Reconcile application messages</h2><p>Enter up to 10 IDs directly, or upload an ID file for a larger reconciliation.</p></div><div className="report-header-actions"><input ref={upload} className="visually-hidden" type="file" accept=".xlsx,.csv" onChange={(event) => void choose(event)} /><Button design="Transparent" icon="download" accessibleName="Download sample Excel file" title="Download sample Excel file" onClick={() => window.open("/api/reconciliations/template", "_self")} /><Button design="Transparent" icon="upload" accessibleName="Upload application message IDs" title="Upload application message IDs" loading={busy} onClick={() => upload.current?.click()} /></div></header><aside className="reconciliation-ribbon"><strong>Limits</strong><span>Direct entry: up to 10 IDs</span><span>Excel or CSV: up to 60 IDs</span><span>1 MB maximum</span><span>Up to 3 systems per run</span></aside>{error && <p className="report-error" role="alert">{error}</p>}<div className="reconciliation-config"><label>Application Message IDs (up to 10)<textarea value={directIds} rows={3} placeholder="611889, 173470" onChange={(event) => { setDirectIds(event.target.value); setPreview(undefined); setResult(undefined); }} /><small>{directCount}/{DIRECT_LIMIT} direct IDs. Separate IDs with commas, spaces, or new lines.</small></label>{preview && <label>Application Message ID column<select value={column} onChange={(event) => setColumn(event.target.value)}>{preview.headers.map((header) => <option key={header}>{header}</option>)}</select><small>{preview.totalRows} uploaded records ready for lookup.</small></label>}<Button className="reconciliation-run" design="Emphasized" icon="play" accessibleName="Run reconciliation" title="Run reconciliation" disabled={!idsToRun().length || !selected.length || (!preview && directCount > DIRECT_LIMIT)} loading={busy} onClick={() => void run()}>Run reconciliation</Button><fieldset className="report-source-picker reconciliation-source-picker"><legend>Systems to reconcile</legend>{sources.length ? sources.map((source) => <label key={source.name}><input type="checkbox" checked={selected.includes(source.name)} onChange={(event) => setSelected((current) => event.target.checked ? [...current, source.name].slice(0, 3) : current.filter((name) => name !== source.name))} /><span><strong>{title(source.name)}</strong><small>{source.description}</small></span></label>) : <p>No compatible systems are currently healthy. Ask an administrator to Ping and enable one.</p>}</fieldset></div>{result && <section className="reconciliation-result"><header><div><p className="section-label">Result</p><h3>Reconciliation report</h3></div><Button design="Transparent" icon="download" accessibleName="Download reconciliation report as Excel" title="Download Excel report" loading={busy} onClick={() => void download()} /></header><div className="reconciliation-table"><table><thead><tr><th>Application Message ID</th>{systems.map((system) => <th key={system}>{title(system)}</th>)}<th>Result</th></tr></thead><tbody>{result.rows.map((row) => <tr key={row.applicationMessageId}><td>{row.applicationMessageId}</td>{systems.map((system) => <td key={system}>{row.systems[system]?.status ?? "—"}</td>)}<td><span className={`reconciliation-status ${row.result}`}>{row.result}</span></td></tr>)}</tbody></table></div></section>}</section>;
}
